# Evals

Six scripts live here: the report checker (`format_check.py`), two test suites over it (`self_test.py`, `mutation_test.py`), the fixture generator those suites depend on (`make_fixtures.py`), the docs-versus-repo scanner (`doc_scan.py`) and the package scanner (`zip_check.py`). No network, no API keys, seconds to run.
Run both suites **and `doc_scan.py`** before shipping any change to `format_check.py`, to the output-format block in `SKILL.md`, or to either README — the suites catch a broken checker, `doc_scan.py` catches a README that still describes the previous one.

```bash
python evals/self_test.py                            # every fixture must land on its expected check id
python evals/mutation_test.py                        # proves each rule is what catches its fixture
python evals/mutation_test.py -v                     # also lists which rule backstops which
python evals/make_fixtures.py                        # regenerate the derived fixtures from their baselines
python evals/make_fixtures.py --check                # verify the fixtures on disk match the generator
python evals/doc_scan.py                             # docs vs repo: counts, paths, commands, links
python evals/format_check.py <report.md>             # check a real report
python evals/format_check.py <report.md> --json      # machine-readable findings
python evals/zip_check.py research-gap-hunter.zip    # before sharing a package
```

Exit codes: `0` clean, `1` violations found, `2` usage or read error. `2` is deliberately distinct from `1` — a missing file is not a clean report.

**Every count in this directory is derived, never written down.** `self_test.py` prints `check 覆蓋率：N / M` from `len(CHECK_DESCRIPTIONS)` against the ids its fixtures pin; `mutation_test.py` prints the same denominator and fails if any declared check has no mutant. An earlier version of this file carried a hand-counted number in prose and it was wrong — in the direction that flattered the repo. Read the numbers off a run, not off this page. The top-level READMEs *do* have to write some counts down, since they are read by people who will never run anything; `doc_scan.py` is what keeps those honest.

## What this can and cannot do

This skill has no runtime engine. It is prose that instructs a model, and the model's output is the product. So the only thing that can be automated here is **whether the report obeys its own format** — never whether the report is true.

`format_check.py` catches: a `DONE` with no paper named, a survivor whose search-evidence field says 「同上」, a block claiming three survivors when two are written, an assumption declared quantified without the ten-field sample frame behind it, a candidate that was generated and then silently vanished, the sentence 「沒有人做過」 — and, in a landscape report, a technique family described with no cost, a family called 飽和 with no search behind the word, a wall whose contributing assumptions do not exist, and the word `CROWDED` appearing in a document that never paid for a verdict.

It also catches **the lines it cannot read**: a table row that never made it into the table, a row whose column count does not match its header, a candidate or family heading it does not recognise. That is a separate promise from the ones above and the one this directory took longest to make good on — see 〈The parser is tolerant about form and never silent〉.

**Two of those things are no longer read out of prose at all.** The section-1 assumption list and the four settlement numbers come from a machine-readable block at the end of the report; the prose is asked only whether the strings the block declares are present in it. See 〈The rgh-block〉 below for what moved, what did not, and what it is worth.

It does **not** catch, and cannot: whether the named paper exists, whether its abstract really says what the report quotes, whether the paper was retracted, whether any query was ever actually run, whether `N`/`M′`/`M`/`K′`/`K` are the real numbers or plausible-looking inventions. A report can be entirely fabricated and pass every check in this directory with a green tick. Existence, retraction and bibliographic correctness are lit-review's job — `verify`, `retract`, `check` — and section 七 of the report format exists precisely so the user can paste the DOI list straight into them.

**Form is checkable, truth is not. Do not let a green run be read as a verified report.**

### Parts of SKILL.md that are deliberately *not* mechanised

Stated here so nobody mistakes the checker's silence for approval:

- the 覆蓋率警告 header line required at tiers 2/3, and the tier strings themselves — the checker only verifies that 文獻工具 is declared and is not a placeholder, not that the wording matches the tier actually used;
- 偵察模式's invariants (1 candidate, 2 searches, only 〔DONE?〕/〔待再查〕) — a recon-mode report is checked as an ordinary report;
- the 第1步-共識 rows in section 六 — nothing requires the consensus searches behind section 一's 主流立場 lines to be logged. The 第1步-推翻A\<k\> rows *are* now required, but only for assumptions the block declares `framed` or `inherited_framed`; that is the whole of the interlock this file reaches into section 一, and both sides of it are pinned by fixtures (`assumption_untraced.md` for the requirement, `inherited_unframed_ok.md` for the exemption);
- the 〈地形來源〉 header line beyond its presence, and whether the wall a block names in `wall` exists in some other file the checker cannot see. The four provenance cases are the four values of the block's `status` field; the checker reads that enum, then asks the prose only whether the matching label string is present;
- whether a CROWDED's three papers really map to three distinct sub-questions, whether a DONE's four-way match holds;
- everything in sections 五 and 七.

And in a landscape report, everything in 〈三、實際上怎麼疊〉 and 〈四、能量在哪裡〉; whether a family really has 3–6 anchors or whether those anchors carry identifiers; whether 〈性質〉 is one of its three allowed values and whether 已經有人在拆 names the work; whether the wall table is ordered by 家族數 descending; whether a 〔涵蓋不足〕 was an honest retreat or a family nobody bothered to search. See 〈Why the landscape rule set is thin〉 below for why that list is long on purpose.

## format_check.py

The checks below are the parts of `SKILL.md` that survive being reduced to a mechanical rule. Each one exists because a plausible-looking report can satisfy the format while quietly skipping the work.

**Two report types, two rule sets.** `SKILL.md` now specifies two outputs: the gap report (`# 研究缺口報告：…`) and the landscape report (`# 領域地形報告：…`). They are different documents, not variants — the landscape report has no candidates, no verdicts and no eliminations, so nineteen of the rules below have nothing to attach to in it, and applying them anyway would produce a page of category errors. `format_check.py` reads the type off the first `# ` heading, falling back to the header 〈模式〉 line, and dispatches to the matching rule set (`Checker` or `LandscapeChecker`). Neither is ever judged by the other's rules. Anything it cannot identify is treated as a gap report, because that was the only type this checker had before landscape existed and a new mode must not silently un-check the old one. The `--json` output names the type in `mode`, so a wrapper never has to guess.

The **mode** column says which rule set a check belongs to: `hunt`, `landscape`, or `both`.

| id | mode | Check | Why |
|---|---|---|---|
| `BLOCK-01` | hunt | A gap report ends with **exactly one** ```` ```json rgh-block ```` fenced block; it is legal JSON; it declares a schema version this checker understands; its top level is exactly `schema` / `settlement` / `assumptions`; and the settlement reconciles — `generated == survived + pending + killed` over non-negative integers | **Structured data has no tolerance question**, which is the entire reason two things were moved into it. There are no shape variants in JSON, so a malformed block is a loud finding rather than a silently dropped line. Zero blocks exits 1 with no degraded mode, because section 一 and the settlement would otherwise be *entirely unchecked while the run still looked like a pass* — and a degraded mode is a sentence that reads "if you would rather not be checked, omit the block". More than one block is the same finding: that single rule is what closes the decoy class, where a second, arithmetically self-consistent block is placed somewhere in the file for the scanner to reach first |
| `ANCHOR-01` | hunt | Every string the block declares that has a prose counterpart must appear **verbatim in the comment-stripped prose** — the assumption's sentence, its id, every number and query of its frame, the settlement arithmetic, the section-2 heading's `生成 N 個 → 存活 M 個`, and the bracket label each non-`framed` status implies. Plus one pass in the other direction: every `預設 A<n>` token in the prose must have a block entry | The block is written by the same model that writes the prose, so without this it would simply be a second, tidier place to lie. Anchors make the block's text fields **self-verifying**: whatever it claims, the reader's page has to contain that sentence. The checker asks the prose one kind of question only — *is this string present* — because **containment cannot silently drop anything**: the answer is presence or absence, and absence is a finding. Comment stripping is mandatory rather than cosmetic; without it a decoy inside `<!-- -->` satisfies containment while the rendered page says something else, which is exactly the mechanism of the settlement decoy this checker used to fall for |
| `PARSE-01` | both | Anything that looks like part of the report's structure but cannot be read is reported: a table row that never entered the table (blockquoted, full-width `｜`, **missing its leading `\|`**, or stranded after a non-table line broke the run), a row whose cell count differs from its header, a candidate heading or a landscape family heading the pattern does not recognise, and **a whole table that cannot be found where one belongs** | Every other rule in this table judges what the report *says*. This one reports what the checker **could not read**, and it exists because the alternative is worse than a miss: an unread row is checked by nothing, and the counting rules then render it as arithmetic — `RECON-01` says 「結算寫已淘汰 6，第四節實際有 5 列」 when the 6 is correct and a row's *shape* is what broke. That message sends the author to change the right number. A separate id is how the author gets sent to the line instead. Its assumption-line arm is **gone**: nothing parses that line any more, so there is no longer a shape it can fail to match |
| `SECT-01` | both | A `## ` section whose contents identify it (its table header, the blocks under it, or the assumption lines in it) but whose **heading text does not** — `SKILL.md`〈輸出格式〉 line 70: 「區段標題與欄位標籤不得改寫」 | This is the rule that had to exist before any of the location fixes were safe to make. A rewritten heading is a form violation in its own right, and until this round it was instead a **silencer**: `## 一、一眼表` → `## 一、總覽表` deleted every glance row, and `LSTAT-01`/`LCOST-01` went dark for every one of them with no finding at all. The finding now lands on the one line that is wrong, and everything underneath is still checked — the heading never decides whether something gets parsed |
| `STRUCT-01` | hunt | 〈存活候選〉〈待確認〉〈已淘汰〉〈檢索紀錄〉 sections all present | A report missing 檢索紀錄 has no audit trail; one missing 待確認 has nowhere to put an undecided candidate, so it drops it |
| `STRUCT-02` | both | Header declares 文獻工具 tier | Tier 2 wording must never be read as tier 0 |
| `COUNT-01` | hunt | The **block's** `settlement.survived` equals the number of `### 候選 N` blocks written in **prose** | The cheapest way to look thorough is to claim candidates that were never written. The two numbers now come from two different places, which makes this a real cross-check; it used to compare a prose heading against prose blocks, and the heading was located by "first match anywhere in the file" |
| `RECON-01` | hunt | The block's `settlement.pending` / `settlement.killed` equal the row counts of sections 三 / 四, and every `C<nn>` appears in exactly one of 二/三/四 | Silent disappearance is the failure mode the 待確認 section was added to prevent; without an arithmetic interlock the section just moves the hiding place. The arithmetic *inside* the settlement is `BLOCK-01`'s; when it does not reconcile these comparisons are skipped, because one defect should not become three messages, two of which send the author to fix rows that are correct |
| `VERDICT-01` | hunt | Each section's verdict/state value is in that section's vocabulary (二: ADJACENT/OPEN/INCREMENTAL, 三: the pending states, 四: DONE/CROWDED only), **and is not blank** | Invented verdicts smuggle in unstated criteria; a pending state in the kill table is an elimination that was never earned. Blank counts as outside the vocabulary because of what blank used to buy: `check_kills` skips a row with no verdict, so `KILL-01`/`KILL-02`/`KILL-03`/`ID-01` all lost their subject and an elimination with an empty 判定 cell passed unread |
| `VERDICT-02` | hunt | Survivors are only ADJACENT / OPEN / INCREMENTAL | A DONE in the survivor list is a contradiction |
| `ASSUM-01` | hunt | Each entry of the block's `assumptions[]`: `id` matches `^A\d{1,3}$` and is unique; `status` is one of exactly four values (`framed` / `impression` / `inherited` / `inherited_framed`); `anchor` is a non-empty sentence; `frame` is present as a key always, an object for the two framed states and `null` for the other two; an object frame carries **all ten** fields — `N` / `query` / `limit` / `Mp` / `pick` / `M` / `refute_query` / `Kp` / `K` / `sample` — with `Mp ≥ 3`, `len(pick) == Mp`, `M ≤ Mp`, `K ≤ Kp`; `wall` is required for the two inherited states; `families` is required for `inherited` | The assumption list is the most hallucinable artifact in the skill: it is written before any candidate exists and everything downstream inherits it. A bare 「檢視 20 篇」 cannot be audited; the split frame can. **All of it is now arithmetic over typed fields rather than five segments of a prose line** — which is what makes rewriting that line unable to switch the rule off. `len(pick) == Mp` is new and was not previously expressible: the old parser only checked that the characters 「pick 索引」 appeared. The inherited exemption is the same principle pointed the other way — a checker that demands evidence of work that was never supposed to happen this round teaches the model to invent it — and it now rides on an enum value rather than on parsing a bracketed label |
| `ASSUM-02` | hunt | A G3 candidate names the assumption it inverts, and that assumption's block `status` is neither `impression` **nor `inherited`** (an inherited wall whose sampling frame has not been paid) | G3 turns an assumption into a research topic. If the assumption was an impression, the whole candidate is an impression wearing evidence — and an unframed inherited wall carries exactly the same force, since landscape's evidence bar is deliberately one notch lower. **Which assumption is inverted still comes from prose** (the candidate's 〈缺口類型〉 field, since candidates did not move into the block); only its eligibility comes from the enum. The two cases are reported separately on purpose: an inherited assumption *is* in the report, so telling its author it does not exist sends them looking for a missing line instead of paying for the one frame that would make the candidate legal |
| `EVID-01` | hunt | Every survivor has a search-evidence field | — |
| `EVID-02` | hunt | That field is not empty or a placeholder (`同上`, `略`, `TBD`, `…`) | — |
| `EVID-03` | hunt | That field contains at least one concrete query string | Queries must be copy-pasteable so the user can re-run them |
| `NEIGH-01` | hunt | 最接近的既有研究 present; if it names a paper it carries an identifier | Without a DOI the user cannot check the claim |
| `KILL-01` | hunt | Every eliminated row names key literature | 每一個淘汰都必須指名殺死它的那篇文獻。There is no exemption any more: a row that cannot name literature is not an elimination, it belongs in 待確認 |
| `KILL-02` | hunt | `CROWDED` names ≥3 papers | `CROWDED` is otherwise the cheapest verdict in the skill and becomes an escape hatch |
| `KILL-03` | hunt | `DONE` carries a verbatim quoted sentence in its reason | A false DONE deletes a viable topic silently and permanently |
| `ID-01` | hunt | Named key literature carries a DOI / arXiv id / S2 corpus id | Makes the `retract` and `verify` handoff mechanically possible |
| `TRACE-01` | hunt | Every candidate in 二/三/四 has a row in 〈檢索紀錄〉 — **except** a 三 row whose state is 〔未驗證〕, or 〔UNSEARCHABLE〕 blocked on terminology. **And** every block assumption whose `status` is `framed` or `inherited_framed` has a row labelled `第1步-推翻A<n>`; `impression` and `inherited` are exempt | The one guard against a report produced with zero searches — and a kill with no logged search is worse than a survivor with none. The exemption exists because those rows are in 待確認 *precisely because nobody searched them*: demanding a log row from them would make the honest report non-conformant and the cheapest way to go green would be to invent a search that never ran. The section-一 arm is new and follows the same logic: a report that declares it ran the refutation search has to produce that row, and one that declares it did not is never asked. **The reasoning did not change; only what the checker reads it off did** — an enum instead of a bracketed label. See `SKILL.md`〈互鎖的例外〉 |
| `TRACE-02` | hunt | 〈檢索紀錄〉 query cells are concrete, not placeholders | Same reason |
| `LANG-01` | both | No assertive non-existence wording anywhere | 「我沒搜到」 is a result; 「不存在」 is a claim. It scans the same comment-stripped text `ANCHOR-01` compares against, with the `rgh-block` removed as well — **the block is not prose**: a hunt block legally contains verdict words and a landscape document has no block at all, so counting it as prose would make one mode miss and the other misfire |
| `TIER-01` | hunt | If the header declares no search was run, no verdict and no elimination may appear | — |
| `LHEAD-01` | landscape | Header declares 〈模式〉 as 領域地形 **and** carries 〈這份報告不做什麼〉 verbatim | A tidy landscape map reads like a novelty conclusion. The disclaimer is the one line that stops that reading, and it is a fixed string precisely so it cannot be softened into 「本報告主要著重於…」 |
| `LVOCAB-01` | landscape | No novelty-verdict token (`ADJACENT`／`OPEN`／`INCREMENTAL`／`DONE`／`CROWDED`) anywhere in the document | **The mode-confusion guard, and the most important of the five.** The failure this mode invites is drifting into verdicts it never paid for: the hunt makes you name a paper and quote its abstract before you may write `DONE`, and a landscape report that writes it has skipped all of that while inheriting the word's authority |
| `LCOST-01` | landscape | Every family carries both 〈買到什麼〉 and 〈付出什麼〉, neither empty nor a placeholder | A family with no stated cost has been described dishonestly — if a method really had no cost it would have cleared the field years ago. One-sided description is the most likely way this mode misleads, and 「還沒查到」 is an accepted value, so the honest answer is always available |
| `LSTAT-01` | landscape | 〈狀態〉 is one of 飽和／活躍／新興／衰退／〔涵蓋不足〕 and is not blank, and anything but 〔涵蓋不足〕 carries the search clause (`回傳 X 筆` plus a verbatim query) in the same field | 「哪些飽和、哪些還在動」 is the question this mode exists to answer, so it is also the sentence most worth faking. Requiring the search inline makes 〔涵蓋不足〕 the cheap answer and 飽和 the paid one — the gradient has to point that way or the mode fills up with confident adjectives |
| `LWALL-01` | landscape | §六 and §二 reconcile both ways: every `F<n>-<字母>` in §二 lands in exactly one wall, every id in 〈來源預設〉 exists in §二, and 〈家族數〉 equals the deduplicated family count | This section is the deliverable — it is what 第 1 步 of the hunt inherits. An assumption written in §二 and missing from §六 is dropped in transit; a wall citing ids that do not exist was imagined rather than pooled. Same defect class as `RECON-01`, same reason for an arithmetic interlock |

`LANG-01` carries a suppression guard: a line containing `≠`, `不代表`, `不得寫`, `並非`, `錯誤示範` and similar is read as *discussing* the forbidden phrase rather than asserting it, so a report may quote the rule it is obeying. The guard is deliberately narrow — see 〈Narrative documents〉 below for why quoting alone is not enough to earn an exemption.

Every finding prints its check id, line number, message and the offending line, and `--json` emits the same fields plus the parse summary (`mode`, `candidate_sections`, `pending_rows`, `kill_rows`, `trace_rows`, `rgh_blocks`, `block_ok`, `settlement`, `settlement_reconciles`, `assumptions`, `unreadable`, `renamed_sections`, `families`, `glance_rows`, `wall_rows`, `report_blocks`) so a wrapper can tell "clean" from "parsed nothing". `unreadable` is the count behind `PARSE-01`: a wrapper that wants one number for "did this file actually get read" reads that one. `rgh_blocks` and `block_ok` are the block's two failure modes kept apart — *absent* and *present but broken* — because a wrapper that folds them together cannot tell a report that predates this version from one whose JSON is malformed. `settlement` and `assumptions` now come from the block, so they are `null` / `0` exactly when the block could not be read. `renamed_sections` is a different thing again and is deliberately not folded into any of them — those lines *were* read, the heading naming them is what is wrong. Line numbers are always ≥ 1: a whole-file defect anchors at line 1 rather than line 0, because a consumer cannot jump to line 0.

### The rgh-block — what moved, what did not, and why only that much

Four adversarial rounds and one naturally-occurring break all landed on the same line: the assumption line in section 一. The diagnosis reached at the end of round 4 is in 〈The scorecard〉 below, and it is not a list of bugs — it is that **regex-parsing a prose report cannot be simultaneously tolerant and never-silent**, and that every patch had *moved* that boundary rather than removed it. So two things, and only two, stopped being read out of prose:

~~~markdown
```json rgh-block
{
"schema": "rgh-block/1",
"settlement": {"generated": 12, "survived": 3, "pending": 3, "killed": 6},
"assumptions": [
{"id": "A1", "status": "framed", "anchor": "…", "frame": {"N": 24, "query": "…", "limit": 24, "Mp": 8, "pick": [0,2,3,5,7,9,11,14], "M": 6, "refute_query": "…", "Kp": 9, "K": 3, "sample": "…"}},
{"id": "A2", "status": "impression", "anchor": "…", "frame": null},
{"id": "A3", "status": "inherited", "anchor": "…", "frame": null, "wall": "W1", "families": ["F1","F2","F3"]}
]
}
```
~~~

Two rules over it, `BLOCK-01` and `ANCHOR-01`, and two principles:

- **EXACT over the block.** Anything that needs to be *computed* — counts, reconciliation, enumerated values, referential integrity, numeric relations — has exactly one source, and it is the block. A malformed block is a loud parse error, not a silent drop, because JSON has no shape variants and therefore no tolerance question.
- **CONTAINMENT over the prose.** Everything about the prose is a presence/absence question. That question **cannot silently drop anything** — the answer is one or the other, and absence is a finding. The tolerance lives entirely in normalization (full/half-width, NFKC, markdown emphasis, whitespace, spaces around commas, case), and normalization only ever makes a string *easier* to find, so its failure direction is a false red.

**Nothing else went in, deliberately.** Candidates, pending rows, the elimination table, the search log, the whole of a landscape report: still prose, still checked by the rules that were checking them before, unchanged. The economy rule that decides the boundary is *does any rule do arithmetic on this* — 可行性, 指導教授適配, 最可能失敗的原因, 最接近的既有研究 stay out because "is it written" is a containment question and containment does not need a schema. The full restructure (candidates, kills, walls, the trace log, all of it) was specified, costed at **+42%** report length, and rejected; the design document that specified it argued against itself and recommended this subset. That recommendation was taken. **If a block ever runs past twenty lines it is carrying something that should have stayed in prose.**

**The honest limit, which is not sold around.** The block is written by the same unreliable narrator this skill exists to check. That **moves** the trust boundary; it does not remove it:

- **Content truth is unchanged.** A block that is valid in every particular, over a report whose every number is invented, still exits 0. The sentence at the top of this file — form is checkable, truth is not — survives the restructure word for word.
- **Containment cannot see negation.** If the block's `anchor` sentence appears in the prose inside 「本研究不採此立場」, the string is found and the meaning is inverted. This is unreachable in principle for a containment test, and it is not fixable by a stricter regex — it would take semantic judgement, which is not what a form checker is.
- **The reverse pass has a blind spot of its own.** An assumption written with drifted wording (`前提 A9`) is not caught by the reverse coverage scan, because that scan keys on `預設 A<n>`. It escapes into *invisibility* rather than into a false claim: not being in the block, it cannot feed a G3 candidate without tripping `ASSUM-02`.

**Containment asks three questions, not one — and the difference is where two regressions lived.** The first version asked only the widest one, 「is this string anywhere in the document」, and `SKILL.md`'s own wording is narrower than that: 「每條預設的 `anchor` → 出現在**第一節那條預設行上**」, 「`impression` → **該預設行**要有〔印象，未驗證〕」. Two constructions that the pre-restructure tree caught went green on that gap and are now closed:

| Question | What it asks | What went green without it |
|---|---|---|
| present | the normalized string occurs in the normalized prose | — (this is the original rule, unchanged) |
| on the line | it occurs on a line that also carries `預設 A<n>` | 〔印象，未驗證〕 moved off `A3` and onto `framed` `A1`: the reader sees both assumptions' force reported backwards |
| in the section | at least one line carrying `預設 A<n>` is **not** inside a section resolved as some *other* kind | the whole of `A1`'s line cut out of section 一 and pasted under a candidate's 〈可行性〉: every anchor still matched, and section 一 no longer held the assumption |

Two properties keep this from being the boundary-moving that rounds 1–4 did. **None of the three can drop anything silently** — each has answers {yes, no} and `no` is a finding, which is the whole reason containment replaced parsing. And **the narrow two never fire alone**: an anchor absent everywhere still gets the original message, so the added questions can only turn a green into a red, never a red into a silence. The section question is deliberately weak in one direction — it asks that *some* mention sit outside the other sections, not that *every* one does, so a candidate that writes 「反轉預設 A1」 in passing is not a red.

The force labels are checked in **both** directions, which is the other half of the same regression: 〔印象，未驗證〕 must be on the line of every `impression` assumption *and* must not be on the line of a `framed` or `inherited_framed` one. A one-sided rule reports a moved label once and lets the assumption it moved *to* be read backwards. `inherited` is deliberately absent from the forbidden side: `SKILL.md`〈互鎖的例外〉 has that line say 「效力同〔印象，未驗證〕」, which is a comparison, not a relabel.

- **Adjacency is now checked for assumption anchors and only for them.** The settlement's two strings are still document-wide questions, correctly — they belong to the header and to section 二's heading, not to any one line.

What it does buy is specific and worth stating exactly once: **the identity of an assumption is now a JSON field rather than the two characters 「預設」 plus a separator**, so no rewriting of that line can make the arithmetic behind it vanish. That is the class of defect that consumed three of four rounds.

#### An empty assumption list is a claim, and only one kind of report may make it

Before the block, "this report skipped 第 1 步 entirely" was an **absence**, and absences are what `STRUCT-01` was never going to see: it requires 存活候選 / 待確認 / 已淘汰 / 檢索紀錄 and nothing more, so deleting every assumption line, writing `"assumptions": []` and retyping the one G3 candidate as G1 exited 0. That is not an adversary's construction — it is the shape an ordinary lazy run takes, and 第 1 步 is both the most expensive step and the only input the G3 generator has.

The block makes it cheap to close, because `[]` is now something the report **says** rather than something it omits. `BLOCK-01` requires a non-empty list, with exactly one exemption, and the exemption costs something: `SKILL.md`〈偵察模式〉 says a recon report writes 表頭〈模式〉逐字「偵察抽樣（非新穎性判定）」, produces no survivor and executes no elimination, and 「沒有跑第 1 步就寫 `"assumptions": []`——空清單是誠實的，漏寫整個區塊不是」. So the exemption needs **two** things, not a declaration: the verbatim header line, *and* `settlement.survived == 0 && settlement.killed == 0` — and those two numbers are themselves cross-checked against the real section-二 blocks and section-四 rows by `COUNT-01`/`RECON-01`. A report that buys the exemption has given up its entire product to get it. Both sides are pinned: `good_recon_report.md` is a compliant recon report and must stay green, `recon_undeclared_empty.md` flips only the header line, and `self_test.py` constructs the second condition in place — a report with three survivors and six kills that declares itself recon — because no single-line edit expresses it.

Tier 3 (no search backend at all) is explicitly **not** this case: it still runs 第 1 步 and writes every assumption as `impression`, which is why `good_nosearch_report.md` carries two of them.

#### The fence is not a place to keep things

A landscape report carries no block by spec (`SKILL.md`: 「地形報告……不寫區塊，也不會因為沒有區塊被判違規」), and for one round a ```` ```json rgh-block ```` fence inside one was the worst thing a document can contain: **an unchecked region**. `parse_report` blanked every such fence out of `rep.prose` and `rep.scan_lines` *before* `rep.mode` was assigned, and `LandscapeChecker` never called `check_block` — so the fence was validated by nothing and scanned by nothing. A fence holding 「…DONE，這個領域沒有人做過，可以確定不存在」 exited 0; the identical sentence as prose exits 1 with `LANG-01` + `LVOCAB-01`. This page argues two paragraphs down that `LVOCAB-01` must have no mention/use exemption because 「that would be the hole that swallows it」 — this was exactly such an exemption, created by an ordering accident.

Two arms close it, and they are separate on purpose:

- **The block is stripped only in the mode that reads it.** The mode is now detected before the prose is built, so a landscape report's fence contents go through `LANG-01` and `LVOCAB-01` like any other line. The reason for stripping was never 「it is fenced」 — it was 「it has already been validated, and it legally contains verdict vocabulary」, and that reason only holds for a hunt report.
- **A block in a landscape report is itself a finding** (`BLOCK-01`). Not because the block is wrong, but because nothing will ever validate it: `LandscapeChecker` has no `check_block` and should not grow one — validating a thing the spec says does not exist concedes that it may.

Generalised, because the specific fix would only have covered the fence that was reported: `PARSE-01` now reports **report structure found inside any fenced block** — a table row, a heading that classifies as a section, a candidate or family heading, a line mentioning `預設 A<n>`, or a field line whose label is in the alias tables. A fence renders as code, so structure inside one is either read by the checker and invisible to the reader, or visible to neither; both are 「content the checker declines to read」, which this file has one answer for. The detector is deliberately narrow — it does not flag every `#` or `|` inside a fence, because a rule that reds a shell comment gets switched off, and switching it off hands the region back. A `#` inside a fence is flagged only if the heading text classifies as one of this format's sections or as a candidate/family heading; a bare `# comment`, a `key: value` line, and a shell transcript were all replayed and stay green. The `json rgh-block` fence is the one exemption in both modes: in a hunt report it is read field by field, and in a landscape report `BLOCK-01` is already pointing at it.

**One deliberate false red, measured.** A fenced ASCII-art table (`| a  | b  |` between `+----+----+` rules) trips the row arm, because that line is character-for-character a markdown row and nothing distinguishes it without guessing. That is the accepted failure direction for this file — a false red is a sentence the author can act on, a false green is not — and it is recorded here rather than argued away. The cheap alternatives all widen the hole: requiring more pipes does not separate the two, and requiring the fence to sit in a table-bearing section hands back exactly the region the rule exists to close.

#### `\d` is not `[0-9]`

`BLOCK_AID_RE`/`BLOCK_WALL_RE`/`BLOCK_FAMILY_RE` used `\d`, which in Python 3 matches the whole Unicode `Nd` category, so `{"id": "A１"}` passed the shape test. What followed was worse than a miss: NFKC runs on the containment side only, so `預設 A１` matched the prose's `預設 A1` while every raw-string comparison — the reverse scan, the G3 `by_id` lookup, the `第1步-推翻A<n>` interlock label — missed, and the run exited 1 with three findings pointing at three lines that were correct and none of them mentioning the id. An id is the key three sections reconcile on, so it is now `[0-9]`, the message names the offending character, and `block_declared_ids` is NFKC-normalized so the downstream rules stay quiet and let `ASSUM-01` say it once. `block_fullwidth_id.md` pins it; its mutant restores `\d` verbatim.

#### The eight rule deletions the design proposed and this repo did not take

The design that specified the full restructure also proposed deleting nine checks on the grounds that structuring makes them tautologies: `PARSE-01`, `COUNT-01`, `COUNT-02`, `VERDICT-02`, `EVID-01`, `EVID-02`, `EVID-03`, `KILL-02`, `KILL-03`. Exactly one was taken — `COUNT-02`, whose deletion row is in 〈Six fixtures were deleted〉 below with its reasoning. **The other eight stay, and the reason is the same sentence for all of them: their data never moved into the block.** Candidates, evidence fields, kill rows and every table are still prose, so the argument for deleting them (a rule over structured data that the schema already enforces is a rule that cannot fire) does not apply — deleting them would delete a check and put nothing in its place. Named here so that a later reader can tell *deferred* from *forgotten*, which is this repo's rule for deletions and was owed to these eight as much as to the six fixtures.

### Nothing that carries checks is located by the text of a section heading

This is the load-bearing sentence of the parser, and it is stated first because the previous round wrote it down — inside `parse_landscape`'s docstring, 「家族靠 `### F<n>` 認，不靠它落在哪一節——節名一旦被改寫，整份報告就會變成『沒有家族因此沒有違規』，那是最壞的一種綠燈」 — and then applied it to two parsers out of six. The glance table and the assumption lines were still found by their `## ` heading, and an adversary got a clean exit 0 out of a broken report by renaming one heading. Writing a principle in a docstring and applying it to the instance in front of you is how a repo ships a false green twice.

So, uniformly: **a table is identified by its header columns, a candidate or family block by the field lines it owns, an assumption line by its own line shape.** Section headings name sections; they never gate parsing. Concretely:

| Structure | Located by | Not by |
|---|---|---|
| landscape glance table | a header carrying 〈家族〉 **and** 〈狀態〉 **and** 買到/付出 | the word 一眼 in the `## ` heading |
| landscape wall table | a header carrying 〈牆〉 and 〈來源預設〉 (checked first — 〈家族數〉 contains 家族) | the word 牆 in the heading |
| kill / pending / trace tables | 〈判定〉+〈關鍵文獻〉 / 〈暫定狀態〉+〈還缺〉 / 〈查詢詞〉+〈回傳筆數〉 | the words 淘汰 / 待確認 / 檢索紀錄 |
| family block | ≥2 of 〈默默預設〉〈狀態〉〈買到什麼〉〈付出什麼〉 in the lines it owns | `### F<n>` alone, or sitting inside a 家族 section |
| candidate block | ≥2 of 〈缺口類型〉〈新穎性判定〉〈搜尋證據〉〈最接近的既有研究〉 | the word 候選 in the heading |
| the assumption list | an entry in the block's `assumptions[]` | any line shape anywhere in the document — **there is no assumption-line parser left** |
| section 一, for `SECT-01`'s message only | a `預設 A<n>` **token** appearing anywhere inside it | the words 共識／預設 in the `## ` heading |

The heading pattern itself is `HEADISH_RE`, which matches `###F1 …` with no space after the hashes. CommonMark does not call that a heading and neither did `HEAD_RE`, which meant the family under it was not merely unparsed but **invisible**: no block, and no line saying a block could not be read. It is now scanned, registered from its contents, and reported.

Three consequences fall out, and all three are the point:

- **A renamed heading produces `SECT-01` and nothing else changes.** Every rule underneath still runs against the same rows. The finding names the line that is actually wrong.
- **Which keyword `classify_section` looks for is now cosmetic.** It supplies a fallback name for a section whose shape says nothing (〈五、下一步〉, 〈七、可查證清單〉) and the "what it would have been called" half of the `SECT-01` message. It cannot make a row disappear.
- **The one remaining use of a heading is additive only.** If a section is *named* as a table section and contains no table header at all, that is `PARSE-01` — the shape-based path cannot see a table that was pasted back from a rendered view, because such a paste has no `|` anywhere, not even in the header. A heading may add a finding; it may never subtract one. For the landscape glance table — the only structure in either report type that nothing else counts — there is a second, shape-only arm: if **no** section anywhere in the document yields a glance table, that is reported regardless of what any heading says. That arm is what covers a heading rename and a de-piped table happening together.

#### What is still located by a string, and why that is correct

Three places name something by its text rather than finding it by shape. Each is listed with the reason, so the next sweep does not have to rediscover the argument — and so that a reason going stale is visible rather than assumed.

- **The report type** (`# 領域地形報告：` / `# 研究缺口報告：`, with the header 〈模式〉 line as a fallback). `SKILL.md` designates the first line as the type discriminator, so this is the spec, not a shortcut. More importantly it **cannot fail silently in either direction**: a landscape report read as a gap report collects `STRUCT-01` four times plus `COUNT-01` and `RECON-01`; a gap report read as a landscape one collects `LHEAD-01` twice. Both are a page of category errors, which is loud and obviously wrong — the opposite of the failure mode this section exists to prevent. A shape-based type detector would be strictly more code for a case that already screams.
- **Header field labels** — 文獻工具, 候選結算, 這份報告不做什麼, 降級聲明. These are located by their own text, document-wide, not by which section they sit in, and each *is* the subject of the rule that reads it (`STRUCT-02`, `RECON-01`, `LHEAD-01`, `TIER-01`). A missing or renamed label is exactly what those rules report. There is no rule underneath them to switch off.
- **A section named as a table section that contains no table.** Additive only, as above: it can produce a finding, never suppress one.

And one place a heading rename genuinely goes unreported: **a section that carries no checks** — 〈三、實際上怎麼疊〉, 〈四、能量在哪裡〉, 〈五、下一步〉, 〈七、可查證清單〉, and the landscape 〈五、檢索紀錄〉 whose table this file does not parse at all. `SKILL.md` forbids renaming those too, but the checker cannot tell a renamed section from an extra one when nothing in it identifies it, and guessing would mean flagging any section it does not recognise. The rule it holds instead is narrower and true: **if the checker can tell what a section is, its heading has to say so.**

### The parser is tolerant about form and never silent

Two halves, and the second is the one that took three rounds of bugs to learn.

**Tolerant about form.** `SKILL.md` is prose and its wording will drift. Labels are matched through an alias table (`搜尋證據` / `檢索證據` / `證據`; `最接近的既有研究` / `最近鄰文獻`), table columns are matched by keyword rather than position, both half- and full-width colons are accepted, `→` / `->` / `至` all parse in the count line, `＝`/`=` and `＋`/`+` both parse in the settlement line, and the `（C03）` id in a candidate heading may be omitted (older reports still parse, they just fail `RECON-01`). Line-level structure accepts a leading `> ` (blockquote), an ordered-list marker as well as `-`/`*`/`+`, bold anywhere the emphasis would naturally fall, and — for candidate and family headings — `###` or `####`, matched after `strip_md`, so `### **候選 1（C01）**：…` reads. Three of those prefixes exist for one reason: **`SKILL.md` displays its own formats inside blockquotes** (第 1 步 shows the assumption line as `> 預設 A1…`, twice), and a reader copies the shape they can see. A checker that rejects the shape its own specification renders is not strict, it is wrong.

**Never silent.** Tolerance has an end, and what happens at the end is the whole question. Until the restructure, everything past the end of the pattern was *dropped*: the line vanished, the counts moved underneath the rules that depend on them, and the run went green. That is the worst behaviour available to a checker. A false red costs somebody five minutes; a false green reports success on a report it did not read, and it teaches the author that a green run means something. So the line held for everything still parsed from prose is: any line that **looks like** part of the structure and does not parse produces a finding naming the canonical form — `PARSE-01` for a row or a structural heading. Where a drop is still tolerated, it is listed in 〈What a malformed line does〉 below with the reason, not left for the next person to rediscover.

**And when the two halves trade off, the false red wins.** That sentence cost a round to learn: round 3 narrowed the assumption look-alike detector so ordinary prose would stop being flagged, and the same narrowing made an em-dash-separated assumption line invisible to *both* patterns. One false red removed, one false green restored. The direction is not symmetric and should not be traded as if it were.

**That trade-off no longer exists for the assumption line, because the parser that created it is gone.** An assumption's identity is a JSON key; its wording, its five numbers and its provenance are `ANCHOR-01` containment questions over the prose. There is no shape to match, so there is nothing to widen or narrow, and the whole class — blockquoted, two adjacent brackets, an ordered-list marker, `｜` instead of `：`, an em dash, 【】/［］/（） brackets, `前提 A1`, `假設 A1`, `Assumption A1` — is **unreachable by construction** rather than closed one variant at a time. `assumption_blockquote_ok.md` is what pins that: the one form `SKILL.md` itself displays, rewritten, still green, with a read-back asserting the assumption is in the block, classified `inherited_framed`, and inverted by a G3 candidate.

The principle still applies everywhere prose is still parsed (tables, candidate and family blocks, headings), and one place it applies in the *new* code is the reverse coverage scan: a prose `預設 A<n>` token with no block entry is `ANCHOR-01`, so a sentence that merely mentions 預設 A1 while the block is honest is safe, but a sentence mentioning 預設 A9 is a false red. That is the accepted direction, stated so the next person does not quietly fix it.

**The four provenance cases are the four values of `status`, not four label shapes.** `framed` (quantified this round), `impression` (`M′ < 3`, or a tier with no abstract-layer tool), `inherited` (a wall carried over from a landscape report, frame unpaid) and `inherited_framed` (carried over, frame paid). Effect: `inherited` is exempt from the frame requirement and from the section-六 interlock and is barred from G3; `inherited_framed` is held to the full ten-field frame and is a legal G3 input. The prose label is still required to be *present* — `ANCHOR-01` asks for 〔印象，未驗證〕, 承接自地形 W…, 支撐家族 …, 已補取樣框 as each status implies — but it is no longer *parsed*, and no message ever tells a report to rewrite 〔承接自地形 W…〕 into 〔印象，未驗證〕: same force, different provenance, and the reader has to be able to see which.

### What a malformed line does — the sweep

The assumption-line pattern was never special. It was the one somebody happened to test, and it failed twice in the same way, so every other parser was put through the same question: *if this line or row is malformed — wrong bullet, blockquoted, extra brackets, full-width punctuation, bold in an unexpected place, a table row with the wrong column count — does it produce a finding, or does it vanish?* The table is the answer, kept here so the next person does not redo it. Its first block of rows is now **history**: that pattern no longer exists, and the rows are kept because a table of closed cases with no record of *how* they were closed is how the next false green gets shipped.

**Two rows of it were wrong when they were written, and an adversary proved it with one-character edits.** They are marked ✗→✓ below. The 「closed」 claim for glance rows and for pending/kill/trace rows covered exactly the three deformations somebody had thought of — blockquote, full-width bar, stranded after a break — and missed the cheapest one of all: **delete the leading `|`**. `parse_table` only consumes lines that `startswith("|")`, and `ROWISH_RE`, the detector that was supposed to catch what `parse_table` refused, is anchored at the start of the line too. So the row was neither consumed nor reported, and if it was the *first* data row the table did not even break, so nothing downstream registered a count change. That is the shape of every false green in this file: the detector and the parser share an assumption, so the detector cannot see what the parser drops.

The lesson recorded, since a table of closed cases is exactly the artifact that invites the next false green: **a row of this table is a claim about a parser, and it is worth what the fixture behind it is worth.** Every ✓ below is now pinned by a fixture, and each fixture is a single-character or single-line edit to a baseline, because that is the size of edit that produced all three of this round's false greens.

| Parser | Malformed how | Before | Now |
|---|---|---|---|
| assumption line — **all rows below are history** | blockquoted `> 預設 A1…`; two adjacent brackets; no bullet; ordered-list marker; `｜` instead of `：`; 【】/（） brackets | dropped, silent (or `ASSUM-02`「指到第一節沒有的預設」, pointing at a line that is there) | **unreachable**: the shape of this line no longer decides anything. Its content is `assumptions[]`; the prose is asked only whether the block's strings are present |
| assumption line | full-width ［］ brackets, which this page promised and the pattern did not contain (half-width `\[`, U+005B, not ［, U+FF3B) | **false red** on a form this page guaranteed; `doc_scan.py` could not see it, because it never compares a doc claim against the contents of a character class | **unreachable**; that promise, and the `BRA_OPEN`/`BRA_CLOSE` constants behind it, are deleted along with the pattern |
| assumption line | **anything else** starting with 預設 + an id, including a table row `\| 預設 A1 \| …`; a separator outside the accepted class (`- 預設 A2——〈…〉`); prose that merely begins with 預設 + an id | round 2 flagged the prose (false red); round 3 added a lookahead to stop that, and the same lookahead made the em dash invisible to **both** patterns — assumptions 3→2, `unreadable` 0, exit 0, zero findings. One false red removed, one false green restored. Round 4 removed the lookahead and kept the prose case red on purpose | **unreachable, as a class.** There is no shape and no look-alike detector, so there is no boundary left to move. The residue is one token scan in the other direction: a prose `預設 A<n>` with no block entry is `ANCHOR-01`, whose failure direction is a false red |
| assumption line | wording drift — `前提 A1`, `假設 A1`, `Assumption A1` | **the only one of these reachable by ordinary mistake, and it was open until now**: both patterns keyed on the literal word 「預設」, so rewriting the line gave assumptions 2→0, `unreadable` 0, exit 0, zero findings | **unreachable.** The id is a JSON key; rewriting the prose makes the block's anchors unfindable, which is `ANCHOR-01`. `self_test.py` performs this exact rewrite on `good_nosearch_report.md` and asserts the result is red |
| assumption line | its section heading renamed, or deleted outright | dropped, silent: assumptions 2→0, exit 0 — and a G3 candidate then drew 「G3 候選 C01 的輸入 A1 不在第一節」, a sentence sending the reader to find a line that was on the page | **unreachable**: the list is not in that section, or in any section. The rename itself is still `SECT-01`, located by a `預設 A<n>` token scan |
| candidate block | bold heading; `####`; blockquoted heading | dropped → `COUNT-01`「宣告存活 3 個，實際只有 2 個」, whose cheapest fix deletes the candidate from the count | all parse |
| candidate block | heading unrecognisable otherwise (`候選三`, a renamed keyword) | as above | `PARSE-01`; the block is still registered from any `C<nn>` in the heading, so the counts stay honest and its fields are still checked |
| candidate block ✗→✓ | `### C01：<題目>`; `### C01 題目：…`; `###候選 1（C01）：…` (the last invisible to `HEAD_RE` entirely — no space after the hashes) | dropped → pure `COUNT-01`/`RECON-01` arithmetic, whose cheapest green fix deletes a real candidate from the settlement | `PARSE-01`; the block is registered from the field lines it owns (〈缺口類型〉〈新穎性判定〉〈搜尋證據〉), so the arithmetic never moves |
| candidate fields (verdict, evidence, nearest study, gap type) | blockquoted; label carrying a parenthetical or stray `*` | dropped → `EVID-01`/`NEIGH-01`/`VERDICT-01`「缺少…欄」 on a field that is present | all parse; those three messages now also name the required shape |
| pending / kill / trace rows | blockquoted row; full-width `｜`; a row stranded after a non-table line broke the run (this one drops **every following row**) | dropped → `RECON-01`/`TRACE-01` rendered it as a count mismatch | `PARSE-01` per row, plus the count finding, which the `PARSE-01` message explains |
| pending / kill / trace rows ✗→✓ | **leading `\|` deleted** — one character | not closed, and this row used to say it was. Neither consumed (`parse_table` wants `startswith("\|")`) nor reported (`ROWISH_RE` is anchored at the line start too). A trace row with a placeholder query lost `TRACE-02` outright; kill/pending rows surfaced only as `RECON-01` arithmetic, whose cheapest green fix decrements the settlement and deletes a real row from the reconciliation | `PARSE-01`, naming the missing leading bar: once the header is known, any unconsumed non-blank line in the section carrying ≥ `max(2, columns−1)` unescaped half-width `\|` is a stray. Half-width only — full-width `｜` is legal content in an assumption line and in a landscape 〈狀態〉 cell |
| pending / kill / trace rows | cell count ≠ header count | silent; values shift a column, and a shift that lands empty in a checked cell disables that check | `PARSE-01` |
| kill row / pending row | 判定 or 暫定狀態 cell blank (or the column missing from the header) | silent — and `check_kills` skipped the row, so `KILL-01`/`02`/`03`/`ID-01` had no subject | `VERDICT-01`, whose message names both causes |
| kill table | header row missing, so the first data row becomes the header | `RECON-01` count off by one | `VERDICT-01` per row (no 判定 column) + `RECON-01`; between them the header is where the author looks |
| landscape family block | `家族 F1：…` prefix; `####`; bold | dropped → `LWALL-01`「指到第二節沒有的預設」 pointing at §六 when §二's heading broke, and that family's cost and status went unchecked | all parse |
| landscape family block ✗→✓ | heading unrecognisable otherwise: `### 遙測綠覆指數` (no id), `### 一、遙測綠覆指數`, `###F1 …` (no space — invisible to `HEAD_RE`) | dropped → `LWALL-01` orphan messages pointing at §六 when §二's heading is what broke, and that family's cost and status went unchecked. The old salvage path existed but was gated on the block being *inside a 家族 section* — and that section is itself identified by its family blocks, so the gate was circular | `PARSE-01`; the family is registered from the field lines it owns, and its `F<n>` comes from the `F<n>-<字母>` ids in its 默默預設 field — which is where `LWALL-01` reads them anyway, so §六 stays correct |
| landscape glance / wall rows ✗→✓ | blockquoted, full-width, stranded, wrong cell count — **and the leading `\|` deleted**, which this row used to claim was closed and was not | silent for the glance table (nothing else counts those rows, so a first-row deletion did not even break the table); `LWALL-01` orphan messages for the wall table | `PARSE-01` for all of them |
| landscape glance row | 狀態 cell blank | silent, while a blank 買到/付出 in the same table raised `LCOST-01` | `LSTAT-01` |
| any section ✗→✓ | its `## ` heading rewritten (`一、一眼表` → `總覽表` / `家族一覽` / `快速對照` / `概覽`; `一、領域共識與未被質疑的預設` → `一、這個領域大家都同意什麼`) | **the worst case in this table**: everything under it stopped being parsed and nothing said so. Glance rows vanished and `LSTAT-01` + `LCOST-01` went dark for every row; assumptions went 2→0 and took `ASSUM-01`/`ASSUM-02` with them. `SKILL.md` line 70 forbids rewriting these headings, so the rename was itself an uncaught form violation | `SECT-01` on the heading; contents located by shape and checked exactly as before. For section 一 the shape is now a `預設 A<n>` token scan, and the rename can no longer take the assumption rules with it whatever happens — they read the block |
| a table section | pasted back from a rendered view — **no `\|` anywhere**, header included | silent: with no header there is no column count, so the missing-leading-bar detector has nothing to compare against, and no shape identifies the section | `PARSE-01`, from the section heading (the one additive use of a heading in the file), plus a document-level arm for the glance table specifically — see the four deliberate drops below |
| report type (`# 研究缺口報告` / `# 領域地形報告`) | H1 blockquoted | H1 missed; saved only by the 〈模式〉 line, and with both mangled a landscape report would be judged by hunt rules — a page of category errors | blockquote stripped before matching |

Four drops are deliberate and stay:

- **A row that was never written.** A family missing from the one-glance table, a candidate with no row anywhere: nothing was malformed, so there is nothing for a parse rule to report. Cross-checking the glance table against the family blocks would be a *content* rule in the shape of `LWALL-01`, and the landscape rule set is thin on purpose (below). The hunt side already has this interlock where it matters — `RECON-01` and `TRACE-01`. **A whole missing table is a different case and is reported**: the glance table is the only structure in either report type that nothing else counts, so a document with no glance table anywhere raises `PARSE-01` — that is existence, not reconciliation, and it costs no content rule.
- **Only the first table in a section is read.** A second table under the same `## ` heading is not a form `SKILL.md` offers, and its rows now surface as `PARSE-01` strays rather than disappearing, which is the honest outcome for a shape the spec does not define.
- **Prose inside a section.** A sentence that is not a field, a row or a heading is not reported. The parser cannot distinguish commentary from a mangled field without guessing, and a checker that flags every sentence trains people to ignore it.
- **A field line with no list marker at all** (`新穎性判定：ADJACENT` with nothing in front) is still read as a missing field rather than an unreadable one. It is reported — `VERDICT-01`/`EVID-01`/`NEIGH-01` fire, anchored on the right candidate block, and those messages now state the required shape. Making bare `X：Y` lines fields would turn every colon in a candidate block into a field-parsing gamble, and the finding is already in the right place.

The line to hold, in one sentence: **tolerant about form, never silent about something it could not read.**

#### What a false green would still take

The previous two rounds both ended believing they were done, so this list is part of the deliverable rather than an appendix. Each item is something a sweep actually found, or a boundary the design accepts on purpose.

The mechanical evidence behind the list: every line of all six hand-written baselines was deformed one at a time in eight ways — leading bar deleted, bar made full-width, line blockquoted, heading renamed, hashes de-spaced, heading bolded, list marker removed — and every result was checked for *counts dropping while the exit code stayed 0*. Zero combinations. That is a real sweep and it is also exactly the shape of sweep whose previous edition missed the one-character case, so read it as "the deformations we thought of", not as coverage.

- **Two independent breakages at once.** A de-piped table (no `|` anywhere, header included) under a heading that was *also* renamed defeats both location paths. For the glance table the document-level arm still catches it; for kill / pending / trace, `STRUCT-01` and `RECON-01` still fire, so it is loud — but their messages point at a missing section and at arithmetic, not at the table, which is the misleading-message failure this round set out to reduce rather than a false green. Nobody has produced a case where two simultaneous breakages are *silent*, and one was searched for.
- **Full-width `｜` used as a row separator with the leading bar also missing.** The stray detector counts half-width `|` only, deliberately: full-width `｜` is legal content inside an assumption line and inside a landscape 〈狀態〉 cell, so counting it would put false reds on correctly written lines. A row using full-width bars *with* a leading one is caught by `ROWISH_RE`; a row with neither is not.
- **Field labels.** `SKILL.md` line 70 forbids rewriting 欄位標籤 as well as section headings, and only the second half of that sentence now has a rule. A renamed *field* label is usually loud in a useful way — `EVID-01`/`VERDICT-01`/`LCOST-01` report the field as absent — but a renamed *column* label in a table is not: the column simply stops mapping, and whether that surfaces depends on which rule reads it. `SECT-01` has no column-label sibling. This is the most likely place the next false green lives.
- **The landscape 〈五、檢索紀錄〉 table is not parsed at all.** No `LTRACE` rule exists, so nothing in it can be malformed in a way this file would notice — including its rows losing their leading bars. That is a gap in the rule set, not in the parser, and it is downstream of the deliberate thinness argument above.
- **The block agreeing with the prose while both are wrong.** Anchors make the block's text fields self-verifying against the page a reader sees; they do not make either true. An `anchor` sitting inside a negating sentence passes — stated in 〈The rgh-block〉 above, unreachable in principle for a containment test, and not fixable by a stricter string test. Scattering an assumption's fragments across the document is no longer in this bullet: containment now also asks whether each one is on that assumption's own line and whether that line sits outside the other sections. What remains of the class is narrower and is recorded in 〈The scorecard〉 below, rows HDR, TBL and DUP.
- **Everything content-level, unchanged.** A report can be internally consistent, perfectly shaped, fully parsed, and entirely fabricated. Nothing here touches that, and no amount of location work — or structuring — will. The block moved the trust boundary from "the checker may not have read this" to "the checker read the report's own account of itself". That is a better place for it, and it is a *different* claim from "safer".

### Why the landscape rule set is thin

Five rules, plus the four that are genuinely mode-independent — `LANG-01`, `STRUCT-02`, `PARSE-01` and `SECT-01`, each pinned in this mode by a fixture of its own, because "this rule also applies to landscape" is worth nothing as a sentence. That is not an unfinished rule set; it is the point of the mode.

Note what the four cross-mode rules are *about*: none of them judges the content of a landscape report. They say the checker could read it, and that it is shaped the way the spec says. Adding readability guarantees costs the mode nothing — a model cannot make a report cheaper to write by leaving a table unparseable — which is why the thinness argument below never applies to them.

The landscape mode exists because the hunt's evidence bars — a verbatim abstract quote and a four-way match for a `DONE`, three papers mapped to three sub-questions for a `CROWDED` — are too heavy for someone who only wants to know where he is. In a measured four-run trial, more than half of all candidates ended in 「待確認」: a defensible verdict, a useless orientation. **Putting a thick rule set on the cheap mode reproduces exactly that outcome**, because a checker is a cost, and the model pays it by leaving fields empty or hedged. So the rules here were chosen against one question — *what does a landscape report look like when it is following the format and already misleading you?* — and there are only a few answers:

1. it quietly starts issuing verdicts (`LVOCAB-01`);
2. it lists what each family buys and stays silent on what it costs (`LCOST-01`);
3. it calls a family 飽和 or 新興 as if that were a fact about the field rather than a count returned by a search (`LSTAT-01`);
4. its final table of walls does not correspond to the assumptions it actually wrote down (`LWALL-01`);
5. it never says out loud that it is not a novelty judgement, and gets read as one (`LHEAD-01`).

Everything else is left alone on purpose. Anchor counts, identifiers on anchors, the 〈性質〉 vocabulary, the ordering of the wall table, whether 〔涵蓋不足〕 was honest — all of them are real rules in `SKILL.md`, and none of them is the difference between a useful map and a misleading one. Checking them would buy accuracy on details while raising the price of the whole mode.

Two consequences worth stating plainly:

- **`LVOCAB-01` has no quotation guard**, unlike `LANG-01`. A landscape report has no reason to write `DONE` even to discuss it, and a mention/use exemption on the single most important check here would be the hole that swallows it. If a future landscape document genuinely needs to quote the vocabulary, it should use the `report-start`/`report-end` fence and keep the quoting prose outside.
- **`LANG-01` grants exactly one exemption in this mode**: the 〈這份報告不做什麼〉 header line, which is required to contain 「不宣稱任何做法沒有人做過」 and would otherwise be flagged on every conformant report. The exemption matches the whole field value against the fixed string, so appending anything to that line puts it straight back under the rule. A checker that cannot be satisfied by its own specification trains the model to ignore it — the same reasoning as the degraded no-search mode below.

**Arms, and what the mutants prove.** Several of these rules have multiple arms: `LSTAT-01` checks vocabulary, the search clause and the verbatim query; `LWALL-01` checks unknown ids, duplicate ids, the family count and orphaned assumptions; `LCOST-01` checks the family blocks and the one-glance table. `mutation_test.py` weakens the arm its fixture actually trips, exactly as `ASSUM-01` does — so a green mutation run proves each *rule* is load-bearing, not that every arm of it is. Do not read it as more than that.

That caveat is not decoration. **Every false green this repo has shipped was an unpinned arm of a rule whose mutant was green.** `PARSE-01` was proved by a wrong-cell-count fixture while the missing-leading-bar arm did not exist; `LSTAT-01` was proved by a family-block fixture while nothing established that its glance-table arm could still find the glance table. `PARSE-01` and `SECT-01` now carry one mutant per arm — read the count off a `mutation_test.py` run, not off this page — and that is the standard a *location* rule has to meet, because an arm of a location rule failing does not weaken one check, it switches off every check downstream of it.

## The scorecard — every case from five rounds, replayed

**Read this section first if you are about to change the parser.** It began as an acceptance test for a restructure that had not happened yet: five defects that reproduced against the tree at the time, deliberately left unfixed, each with its exact repro so it could be re-run rather than re-discovered. The restructure happened — the section-一 assumption list and the settlement moved into the `rgh-block` — and the list was replayed. The round after it closed the four holes the block itself opened, the two coverage regressions the restructure caused, and one pre-existing hole. **This is the published end state of all five rounds, not a to-do list.** Every row below is a measured replay against *this* tree, each edit applied on its own to a compliant baseline, and every row that is still reachable carries the edit that produces it — so a maintainer can re-run the whole list instead of re-discovering it. It is not the whole ceiling: 〈What a false green would still take〉 above holds the boundaries the design accepts on purpose, and 〈The rgh-block〉 holds the ones the block itself introduces.

Why they were not patched one at a time. The diagnosis round 4 arrived at is that parsing a prose report with regexes, under a requirement to be simultaneously **tolerant** (`SKILL.md` is prose, a model's wording drifts, and a checker that red-flags every variation gets ignored) and **never silent** (a dropped line is a false green, which is worse than a miss), is a contradiction: every patch *moves* the boundary between the two rather than removing it. The em-dash regression is the proof — round 3's lookahead, added to stop a false red, bought back exactly the false green the rule existed to close.

The bar this section sets: an item is closed only if it is **unreachable as a class**, not fixed as an instance. `closed` in the status column always means that, and it never means 「a new pattern now matches this input」.

**How to read the status column.**

| Status | Means |
|---|---|
| `closed` | Unreachable by construction: there is nothing left to attack, not a new rule that catches this one input |
| `open (decision)` | Reachable today. Closing it was costed and declined, and the row carries the argument. Nobody is coming back to it |
| `open (limit)` | Reachable today and **not** closable by a string test at all. Closing it would take semantic judgement, which is not what a form checker is |
| `false red` | The checker fires on a document that is fine. Recorded rather than argued away, because this file's whole trade-off is that a false red is the cheap direction |

**No count is written in this section.** The rows are the source; `doc_scan.py` derives the totals off this table and checks them against the two top-level READMEs, which are read by people who will never run anything. Add a row here, or re-classify one, without the READMEs following, and a `doc_scan.py` run goes red.

<!-- scorecard: start -->

### Closed by construction

| # | Status | The false green | The edit that produced it | Replayed against this tree |
|---|---|---|---|---|
| FG-2 | `closed` | **Settlement / count decoy takes the first match, including inside an HTML comment.** `parse_report` scanned for the first line matching 〈候選結算〉 or 〈生成 N → 存活 M〉 and broke. Nothing required that line to be in the header, or to be visible when the markdown is rendered | Prepend `<!-- **候選結算**：生成 12 ＝ 存活 3 ＋ 待確認 2 ＋ 已淘汰 6 -->` to [`recon_mismatch.md`](fixtures/recon_mismatch.md); prepend `<!-- 生成 12 個 → 存活 2 個 -->` to [`count_mismatch.md`](fixtures/count_mismatch.md) | **exit 1 — `RECON-01` and `COUNT-01` respectively.** Three independent mechanisms, any one of which alone closes it: those four numbers have exactly one source and it is the block, so there is no first-match race left to win; there must be exactly one block; and containment runs on comment-stripped text, so a string living only inside `<!--…-->` is not present in the prose at all |
| FG-2b | `closed` | **The decoy's obvious successor: a second, arithmetically self-consistent block** | Prepend a complete, self-consistent ```` ```json rgh-block ```` to `count_mismatch.md` | **exit 1, `BLOCK-01`** (「found 2 blocks, must be exactly one」). Decoy-first, decoy inside an HTML comment and decoy inside a `~~~` fence were each replayed and are each exit 1: cardinality is evaluated over the raw file text, so no ordering and no wrapper yields one block while a second exists |
| FG-3 | `closed` | **The report-block markers excise content and raise nothing.** `apply_report_blocks` blanks everything outside `report-start`/`report-end`. It was built for teaching documents; nothing distinguished that use from carving a hole in a report | With `report-start` at the top of `good_report.md`, put `report-end` on the line before `- 預設 A1：…` and `report-start` on the line after — three markers, because the two-marker variants are loud | **exit 1, `ANCHOR-01`.** The excised line takes the block's anchor strings out of the prose, and containment's answer set is {present, absent} with absent a finding. Closed for everything the block's anchors point at; the excision mechanism itself is untouched, which is row FG-3r below. Hiding the **block** with the markers is exit 0 and that is correct — the block is extracted from the raw text *before* any windowing, so nothing was hidden from checking |
| R2-a | `closed` | **The assumption line written as a blockquote** — round 2 | `- 預設 A1：…` → `> 預設 A1：…` in `good_report.md` | **exit 0, and exit 0 is now the correct answer**: that is a legal shape and no rule reads the shape. Proof the rule went green rather than dark — the same edit plus `"Mp": 8`→`2` in the block is **exit 1, `ASSUM-01`**; plus a `status` flipped to `impression` while a G3 still inverts it, **exit 1, `ASSUM-01` + `ASSUM-02`**; plus its trace row deleted, **exit 1, `TRACE-01`**. Pinned green by `assumption_blockquote_ok.md` |
| R2-b | `closed` | **Two adjacent bracket labels** — round 2 | `〔印象，未驗證〕` → `〔印象，未驗證〕〔待補〕` on `impression_as_g3.md` | **exit 1, `ASSUM-02`.** The label is decoration now; an assumption's force is the block's `status` enum, and `BRACKETED_RE` survives only inside `_clean_verdict`, which is a different rule on different text |
| R4 | `closed` | **An em-dash separator** — round 4, and the proof that patching moves boundaries: round 3's lookahead removed a false red and made this line invisible to *both* patterns | `- 預設 A1：〈…` → `- 預設 A1——〈…` | **exit 0 (correct).** The same edit plus `"K": 3`→`99` in the block is **exit 1, `ASSUM-01`**; plus `"Mp": 8`→`2`, the same. The lookahead is gone along with `ASSUM_LINE_RE`/`ASSUM_LOOKALIKE_RE`: there is no separator left for a separator variant to defeat |
| DRIFT | `closed` | **Wording drift on the assumption line** — `前提 A1`, `假設 A1`, `Assumption A1`. Both patterns keyed on the literal word 「預設」, so rewriting the line gave assumptions **2→0**, exit **0**, zero findings. **The only case in this table reachable without deliberate construction, and therefore the one that mattered** | Rewrite `- 預設 A1：` in [`good_nosearch_report.md`](fixtures/good_nosearch_report.md) three ways | **exit 1, `ANCHOR-01`, all three.** An assumption's identity is a JSON key, so rewriting the prose makes the block's anchors unfindable, and absence is the only other answer containment has. `self_test.py` runs the 前提 variant on every run, so it cannot silently regress |
| R3-a | `closed` | **Section 一's heading renamed** — round 3. Everything under it stopped being parsed and nothing said so | `## 一、領域共識與未被質疑的預設` → `## 一、本研究的出發點` on `good_report.md` | **exit 1, `SECT-01` alone.** Nothing underneath goes dark: the same rename on `impression_as_g3.md` is `SECT-01` + `ASSUM-02`, and renaming 〈六、檢索紀錄〉 on `untraced_candidate.md` is `SECT-01` + `TRACE-01` |
| R3-b | `closed` | **The landscape glance heading renamed** — the worst row of the sweep table: every glance row vanished and `LSTAT-01` + `LCOST-01` went dark for all of them, with no finding at all | `## 一、一眼表` → `## 一、總覽` on `good_landscape.md` | **exit 1, `SECT-01`.** The same rename on `landscape_no_cost.md` is `SECT-01` + `LCOST-01`. Tables are located by their header columns, never by the heading text |
| B-1 | `closed` | **The fence was a blind spot in landscape mode.** A ```` ```json rgh-block ```` fence inside a landscape report was validated by nothing (`LandscapeChecker` has no `check_block`) *and* stripped out of the prose and the vocabulary scan, because the strip ran before the mode was known — an unchecked, unscanned region of the document | Append to `good_landscape.md` a fence holding 「F3 …DONE，這個領域沒有人做過，可以確定不存在」; separately, append one holding `{ this is not json at all` | **exit 1 — `BLOCK-01` + `LANG-01` + `LVOCAB-01`, and `BLOCK-01` for the broken one.** The strip now runs after `detect_mode` and only for hunt reports, so the text is scanned like any other line; and a block in a landscape report is a finding in itself, because nothing will ever validate it. Pinned by `landscape_stray_block.md` |
| B-2 | `closed` | **Report structure hiding in a fence, generally.** The specific fix above would have covered only the fence that was reported | Wrap `A3`'s assumption line in a ```` ```text ```` fence, leaving it in section 一 with every anchor still matching; separately, the same in a `~~~` fence | **exit 1, `PARSE-01`, both.** Any fence except the `rgh-block` is scanned for table rows, classifiable headings, candidate/family headings, `預設 A<n>` mentions and known field labels. Pinned by `fenced_assumption_line.md` and `tilde_fenced_assumption_line.md`: `find_fenced_blocks` is backtick-only on purpose, but the structure scan asks a different question — 「does the reader see code here」 — and `~~~` renders identically, so recognising one and not the other would leave a one-character bypass |
| B-3 | `closed` | **A regression the restructure caused: the assumption line relocated out of section 一.** Every anchor still matched, because containment only asked 「is this string in the document」 | Cut `A1`'s whole line from section 一 of `good_report.md` and paste it into candidate `C01`'s 〈可行性〉 paragraph | **exit 1, `ANCHOR-01`** (the pre-restructure tree raised `ASSUM-02`; the round-5 tree was exit 0 — that is what makes it a regression). Containment now also asks whether some line carrying `預設 A<n>` sits outside the sections resolved as *other* kinds. Pinned by `anchor_line_relocated.md`. Pasting it into 〈五、下一步〉 instead is **exit 1, `SECT-01`** — that section then shape-resolves as 第一節 and its heading is the thing that is wrong |
| B-4 | `open (decision)` | **A regression the restructure caused: the force label moved to the wrong assumption.** Both assumptions are then read backwards by anyone looking at the page | Remove 〔印象，未驗證〕 from `A3`'s line in `good_report.md` and append it to `framed` `A1`'s | **exit 1, `ANCHOR-01`** for the same-line case (the pre-restructure tree raised `ASSUM-01`; the round-5 tree was exit 0). Both sides pinned: `anchor_label_off_line.md` for the leaving, `anchor_label_wrong_status.md` for the arriving — one side alone reports a moved label once and lets the assumption it moved *to* keep the wrong force. **This row said `closed` when it was written and that was wrong: the label is one newline from bypass.** `_check_status_decorations` inspects only lines that themselves match `PROSE_AID_RE`, so a label placed on the *following* line escapes — while a markdown lazy continuation still renders it inside the same list item, i.e. a reader sees exactly the report the rule exists to prevent. Reproduced against this tree, exit 0. Not fixed: widening to "the next line too" moves the boundary rather than removing it, which is the pattern all five rounds ran on, and the honest end state of this list is a recorded limit rather than a sixth patch |
| B-5 | `closed` | **第 1 步 skipped entirely.** Pre-existing — the pre-restructure tree was green on it too — and the one hole an ordinary lazy run falls into rather than an adversary, since `STRUCT-01` requires only 存活候選／待確認／已淘汰／檢索紀錄 | Delete every `- 預設 A…` line from `good_report.md`, set `"assumptions": []`, retype the one G3 candidate as G1; also replayed with section 一 deleted outright | **exit 1, `BLOCK-01`.** An empty list is now a claim, and only 偵察模式 may make it — while *also* declaring no survivor and no elimination, two numbers that `COUNT-01`/`RECON-01` cross-check against real rows. Both sides pinned: `good_recon_report.md` green, `recon_undeclared_empty.md` red, plus an in-place construction in `self_test.py` for the second condition |
| B-6 | `closed` | **A full-width digit in an id.** `{"id": "A１"}` passed `\d`, which matches Unicode `Nd`. Never a false green — it exited 1 — but with three findings on three lines that were correct and the id named in none of them | Rewrite `A3`'s id in `good_report.md`'s block as `A３` | **exit 1, `ASSUM-01` alone**, naming the offending character. `[0-9]` in the three id patterns, and `block_declared_ids` NFKC-normalized so the downstream rules stay quiet and let one rule say it once. Pinned by `block_fullwidth_id.md`, whose mutant restores `\d` verbatim |

### Still reachable, and shipping that way

| # | Status | The false green | The one-line edit that produces it | Replayed against this tree | Why it is not being patched |
|---|---|---|---|---|---|
| FG-1 | `open (decision)` | **Split-table tail swallowed by an inserted heading.** Nothing counts the rows of the landscape one-glance table, so rows that fall out of it are unread and unreported | In `good_landscape.md`, insert one line — `## 一之二、（續）` — immediately before the `\| F5 行動裝置定位軌跡 …` glance row | **exit 0, zero findings** | Landscape tables. The subset moved nothing in a landscape report, and reaching this means rewriting the table parser to serve a rule set that is thin **on purpose** (〈Why the landscape rule set is thin〉). That ratio was judged wrong three rounds ago and nothing since has changed it |
| FG-4 | `open (decision)` | **The lost-leading-pipe detector counts half-width `\|` only**, so a row that lost its leading bar *and* uses full-width bars is neither consumed nor reported | In [`glance_row_lost_pipe.md`](fixtures/glance_row_lost_pipe.md) — already missing its leading bar — replace the remaining `\|` on that row with full-width `｜` | **exit 0, zero findings.** Control: the fixture as shipped is exit 1, `PARSE-01` | Full-width `｜` is legal *content* in an assumption line and in a landscape 〈狀態〉 cell, so counting it would put false reds on correctly written lines. Reachable by IME drift, not only by an adversary, which is why it is recorded rather than dropped |
| FG-5 | `open (decision)` | **`LWALL-01`'s 家族數 reconciliation returns silently when the cell holds no ASCII digit** — `re.search(r"\d+", cell)` with no match returns without a finding | In `good_landscape.md`, rewrite `W3`'s 家族數 from `2` to `三` while leaving 〈來源預設〉 at two families | **exit 0, zero findings.** Control: `9` is exit 1, `LWALL-01` | Landscape again. The fix the design specified — compute 家族數 from the sources instead of reading it — was part of the full restructure that was costed at +42% and declined |
| FG-3r | `open (decision)` | **The report-block markers excising a line the block's anchors do *not* point at.** The excision mechanism is intact; only what the anchors reach is covered | The three-marker construction of FG-3, applied around a `- **可行性**：…` line in `good_report.md` | **exit 0, zero findings** | The markers' scope is section 一 and the settlement, which is exactly the scope of the subset that moved into the block. The design also proposed deleting the markers outright; that deletion was deferred, and `narrative_wrapper.md` is why they exist |
| A9 | `open (decision)` | **A drift-worded assumption that is absent from the block.** The reverse coverage scan keys on `預設 A<n>`, so a line written 「前提 A9」 and left out of the block is seen by nothing | Append `- 前提 A9：〈完全沒進區塊的一條預設〉` to section 一 of `good_report.md` | **exit 0, zero findings.** Control: the canonical `- 預設 A9：…` is exit 1, `ANCHOR-01` | It escapes into **invisibility**, not into a claim: not being in the block, it cannot feed a G3 candidate without `ASSUM-02`. Widening the scan's vocabulary would catch the three drift words somebody thought of and not the fourth — which is the tolerance argument five rounds failed to converge on |
| HDR | `open (decision)` | **An assumption line that sits *only* in the header region**, above the first `## ` | Cut `A1`'s line from section 一 of `good_report.md` and paste it above the first `## ` heading | **exit 0, zero findings** | The section question asks that a mention sit outside the sections resolved as some *other* kind, and the header is not a section. Tightening it to 「must be inside 第一節」 trades this for a **silent drop** whenever 第一節 cannot be located — precisely the trade this round refused |
| TBL | `open (decision)` | **An assumption line rewritten as a one-row table inside 第一節** | Rewrite `- 預設 A1：…` as `\| 預設 A1：… \|` in place | **exit 0, zero findings** | Both narrow questions are satisfied — it is on one line, and that line is in 第一節 — and the reader still sees the text. What changed is the line's *shape*, and no rule reads the shape of that line any more; that property is what three rounds were spent buying |
| REL | `open (decision)` | **An `inherited` assumption's line relabelled 〔印象，未驗證〕**, which `SKILL.md`〈互鎖的例外〉 forbids in prose | Append 〔印象，未驗證〕 to `A2`'s line in `good_inherited_report.md` | **exit 0, zero findings** | That same line is *required* to say 「效力同〔印象，未驗證〕」, so the two differ by intent and not by string, and `inherited` is deliberately off the forbidden side of the label rule. Narrowing it would put a false red on every conformant bridge report. Bounded: both statuses are barred from feeding G3 either way |
| DUP | `open (decision)` | **A second, contradictory `預設 A<n>` line carrying no force label** | Add `- 預設 A1：〈其實這個代理量什麼都代表不了〉` inside 第一節 (or inside a candidate block) of `good_report.md` | **exit 0, zero findings.** Placing it under 〈五、下一步〉 instead is exit 1, `SECT-01` — that section then shape-resolves as 第一節 | The line and section questions ask that **some** line satisfy them, deliberately: tightening to *every* line red-flags a candidate that writes 「反轉預設 A1」 in passing. A duplicate that carries a *label* is caught, because the label rule is per-line |
| NEG | `open (limit)` | **Anchor text inside a negating sentence.** The string is found and the meaning is inverted | Rewrite `- 預設 A1：〈…〉` as `- 預設 A1：本研究不採此立場——〈…〉` | **exit 0, zero findings, and it will stay that way** | Containment is a presence test and presence has no polarity. Catching it takes semantic judgement, which is not what a form checker is. Named by the design as unreachable in principle **before** anybody tried it — recorded rather than sold around |
| SUF | `open (limit)` | **The visible settlement line carrying an extra term**, so the rendered arithmetic is false while every anchored substring is present | Rewrite the header line as `**候選結算**：生成 12 ＝ 存活 3 ＋ 待確認 3 ＋ 已淘汰 6 ＋ 未分類 4` | **exit 0, zero findings** | Containment cannot see a suffix for the same structural reason it cannot see negation. Bounded: 存活／待確認／已淘汰 are each cross-checked against the real row counts, so only the extra term is unpoliced |

### One accepted false red

| # | Status | What fires | The edit that produces it | Replayed against this tree | Why it stays |
|---|---|---|---|---|---|
| FR-1 | `false red` | **A fenced ASCII-art table** trips `PARSE-01`'s in-fence row arm | Append a fence containing `\| a  \| b  \|` between `+----+----+` rules to any report | **exit 1, `PARSE-01`** | That line is character-for-character a markdown row and nothing separates the two without guessing. This is the accepted failure direction of the whole file — a false red is a sentence the author can act on. Every cheap suppression widens the hole the rule closes: requiring more pipes does not separate them, and requiring the fence to sit in a table-bearing section hands back exactly the region the rule exists to close |

<!-- scorecard: end -->

**Two things this scorecard is not.** It is not a list of bugs deferred for lack of time: every open row carries the argument for leaving it, and the two marked `open (limit)` cannot be closed by a string test at all. And it is not exhaustive — it is what five adversarial passes found, and four of those passes ended believing their list was complete. **This is the last patching round.** The reason is the same one that produced the block: each of the first four rounds was wrong in the same way, because a patch moves a boundary rather than removing it. What is left is published instead of fixed, because publishing it is the honest end state.

The right way to read a green run has not changed: **no rule that could see this report found a defect in it.**

## Narrative documents — the report block

`examples/worked_example.md` is not a report. It is a teaching walkthrough that *contains* report fragments, and its prose quotes 「沒人做過」 in order to warn against writing it. Running the checker over it as if it were a report produces category errors: `STRUCT-02` fires because the header sits below a `## 第 0 步` heading, `COUNT-01` compares a narrative sentence against zero candidate blocks, and `LANG-01` fires three times on the very sentences that teach the rule.

Two fixes were available and one of them is wrong:

- **Rejected: exempt quoted text from `LANG-01`.** It would make the example green at the cost of the check. A report could then write 「我們可以確定地說：「這個題目沒有人做過」」 and pass. The mention/use distinction is not recoverable from punctuation, and `LANG-01` guards the single most consequential sentence type in the whole output.
- **Adopted: check only a delimited report block.** A document may mark where its report actually starts and ends:

```markdown
<!-- format-check: report-start -->
# 研究缺口報告：…
…
<!-- format-check: report-end -->
```

When at least one such block exists, `format_check.py` checks **only** the lines inside the blocks, and nothing else in the file — teaching prose, error demonstrations, before/after comparisons are all invisible to it. Lines outside are blanked rather than removed, so **reported line numbers still refer to the original file**. A `report-start` with no matching `report-end` runs to end of file. Multiple blocks in one document are all checked.

Note what this does *not* do: it does not weaken a single check. Inside the block every rule applies at full strength, including `LANG-01`. The example gets no leniency about its own report — it gets a boundary around the part that claims to be one. `narrative_wrapper.md` is the fixture: `good_report.md` wrapped in a document whose surrounding prose contains 「沒人做過」, expected to be clean.

**These markers are not the `rgh-block`, and one ordering rule keeps them from interfering.** The `rgh-block` is extracted from the **raw file text, before any `report-start`/`report-end` windowing**. Reverse that and the markers become a way to hide the block from the checker — which would still be loud (`BLOCK-01` fires either way) but with the wrong message: 「這份報告沒有區塊」 when the block is right there and a marker excluded it. Both orderings were replayed; the current one reports nothing for a document that fences its block out, because nothing was actually hidden from checking. Excising a line the block's anchors point at is a different matter and is `ANCHOR-01` — see 〈The scorecard〉, rows FG-3 and FG-3r.

`self_test.py` treats the examples accordingly: it walks `examples/` off disk rather than from a hard-coded filename, and for every `.md` there, if the file carries the marker it **must** pass the checker or the suite fails; if it does not, the suite prints a warning saying that file is unchecked and moves on. It never pretends a file was verified. Every example currently carries the marker and passes, so those assertions are live — a future edit to an embedded report will turn `self_test.py` red. Reading the directory instead of naming a file is the fix for the obvious next failure: a new walkthrough gets added, nobody remembers this list, and it goes unchecked while the suite stays green.

## fixtures/

The baselines are hand-written:

Every gap-report baseline now ends with an `rgh-block`. That is the one edit the restructure made to the hand-written files; **not a word of their prose changed**, which is the same migration a real report needs.

- `good_report.md` — a full, realistic, fully compliant zh-TW report in the seven-section format: three survivors, three pending rows, six eliminated rows, twenty-two search-log rows, two `framed` assumptions and one `impression`.
- `good_nosearch_report.md` — the tier-3 degraded report (see below), written straight from `SKILL.md`'s rules for that mode.
- `chinese_index_na.md` — a second full report on an unrelated topic, written to pin the third 〈中文索引〉 header value 「不適用（題目非在地界定，使用者不在華語學術體系）」. It is hand-written rather than generated so that the value is exercised by a report that genuinely has no locally-bounded candidate, instead of by an edit to one that does.
- `good_landscape.md` — the landscape baseline: seven technique families for measuring urban green-space exposure, one of them deliberately marked 〔涵蓋不足〕 with 「還沒查到」 for its cost, and a four-row wall table into which every one of the ten `F<n>-<字母>` assumptions lands exactly once. It is a **base**, not a variant of the first: the landscape fixtures are derived from it, not from `good_report.md`.
- `good_recon_report.md` — the **recon** baseline (`SKILL.md`〈偵察模式〉): one candidate, two searches, everything in 待確認 as 〔DONE?〕, no survivor and no elimination. It is the only report shape allowed to hand in `"assumptions": []`, which is the whole reason it is a base rather than a variant — reaching it from `good_report.md` would mean deleting three assumption lines and their anchors at once, three dimensions instead of one.
- `good_inherited_report.md` — **the bridge**: a hunt report whose header carries 〈地形來源〉 and whose section 一 holds three kinds of assumption at once — one quantified this round (`framed`, `A1`), one inherited from wall W1 and left unframed (`inherited`, `A2`), and one inherited from W3 whose sampling frame was then paid (`inherited_framed`, `A3`). `A3` is inverted by a G3 candidate, which is the full path `SKILL.md` permits and the one the checker used to reject. It is a base rather than a derivation because the three kinds have to coexist in one file for a one-line edit to isolate any of them.

**Everything else is generated by `make_fixtures.py` from one of the four bases**, one substantive edit each, so "only one dimension is broken" holds by construction rather than by eyesight. The generator refuses to run if the text it is told to replace does not occur exactly once, and `self_test.py` re-runs it in `--check` mode and fails if any fixture on disk has drifted from the generator — a hand-edited fixture would eventually break a second dimension and nobody would notice. Which base each derived fixture came from is declared once, in `make_fixtures.derived_bases()`, and `self_test.py` imports it rather than keeping a second copy — two hand-maintained mappings drift, and the day they drift nobody notices.

**A derived fixture edits the prose or the block, never both.** That is not a style rule: `self_test.py` allows a derived file to differ from its baseline by at most two lines, and a coordinated prose+block edit needs three or four. It also happens to be the right constraint — an edit that breaks both sides raises two ids and stops being diagnostic. Two generator verbs exist for the block specifically, `drop_block` and `dup_block`, so that neither the "absent" nor the "two of them" fixture has to embed a copy of the block's contents and drift from it later.

Three fixtures are exempt from the two-line count, and the exemption is from the *proxy*, not from the property — all three are still **generated**, so 「only one dimension」 remains constructed rather than eyeballed. Each states its own reason in `self_test.py`: `narrative_wrapper.md` wraps a whole report in teaching prose; `landscape_stray_block.md` adds a fenced block, which is three lines because a fence cannot be fewer; `tilde_fenced_assumption_line.md` opens and closes with the same string `~~~`, neither of which occurs in the baseline, so the proxy counts one construct twice — its ```` ``` ```` twin counts as one only because the baseline happens to contain a bare ``` already. That last one is worth reading as a warning about the proxy rather than about the fixture. The line count is still the only mechanical guard on single-dimension-ness, so a fourth entry should have to argue for itself the way these three do.

| Fixture | Broken dimension | Expected |
|---|---|---|
| `good_report.md` | — | exit 0, no findings |
| `good_nosearch_report.md` | — (the degraded mode: no search tool was available) | exit 0, no findings |
| `good_recon_report.md` | — (the recon mode: the only shape allowed to hand in `"assumptions": []`) | exit 0, no findings |
| `bracketed_verdict_ok.md` | — (bracketed state 〔待驗證〕 followed by a note) | exit 0, no findings |
| `narrative_wrapper.md` | — (report block inside a teaching document) | exit 0, no findings |
| `unsearched_pending.md` | — (a partially-searched run: a candidate honestly parked in 待確認 as 〔未驗證〕 is exempt from the search-log interlock) | exit 0, no findings |
| `chinese_index_na.md` | — (hand-written second baseline; 〈中文索引〉 = 不適用) | exit 0, no findings |
| `good_inherited_report.md` | — (the bridge baseline: an `inherited_framed` wall legally inverted by a G3 candidate) | exit 0, no findings |
| `inherited_unframed_ok.md` | — (the `inherited` wall sits in the report feeding no G3: no sample frame demanded, no search-log row demanded) | exit 0, no findings |
| `assumption_blockquote_ok.md` | — (`A3`'s prose line written the way `SKILL.md` 第 1 步 displays it, `> ` and all — now pinning the stronger claim that **no** line shape decides anything) | exit 0, no findings |
| `block_absent.md` | The whole `rgh-block` deleted; not a word of prose changed | exit 1, `BLOCK-01` |
| `block_duplicate.md` | The same block appears twice | exit 1, `BLOCK-01` |
| `block_bad_json.md` | The `"schema"` line loses its trailing comma, so the block is not legal JSON | exit 1, `BLOCK-01` |
| `block_settlement_mismatch.md` | The block's 已淘汰 says 5; 12 ≠ 3 + 3 + 5 | exit 1, `BLOCK-01` |
| `block_status_unknown.md` | `A3`'s `status` is `unverified`, outside the four-value enum | exit 1, `ASSUM-01` |
| `block_fullwidth_id.md` | `A3`'s id is written `A３` with a full-width digit — `\d` used to accept it, and the three findings that followed all pointed at correct lines | exit 1, `ASSUM-01` |
| `recon_undeclared_empty.md` | The recon baseline's header stops declaring 偵察抽樣 while the block still hands in `"assumptions": []` — the other side of `good_recon_report.md` | exit 1, `BLOCK-01` |
| `anchor_prose_missing.md` | The prose sentence for `A1` gains two characters the block does not have | exit 1, `ANCHOR-01` |
| `anchor_in_html_comment.md` | Same edit, with the block's version of the sentence hidden in an HTML comment on that line — green without comment stripping, which is why comment stripping is mandatory | exit 1, `ANCHOR-01` |
| `anchor_orphan_assumption.md` | The prose adds a `預設 A4` the block does not list (the reverse direction) | exit 1, `ANCHOR-01` |
| `anchor_line_relocated.md` | `A1`'s whole line is cut from section 一 and pasted under a candidate's 〈可行性〉 — every anchor still matches, and section 一 no longer holds the assumption | exit 1, `ANCHOR-01` |
| `anchor_label_off_line.md` | 〔印象，未驗證〕 is removed from `A3`'s line while the fixture's own note still contains it, so the string is present in the document and absent from the line | exit 1, `ANCHOR-01` |
| `anchor_label_wrong_status.md` | 〔印象，未驗證〕 is additionally hung on `A1`, whose block `status` is `framed` — the reverse side of the same rule | exit 1, `ANCHOR-01` |
| `missing_trace_section.md` | 〈六、檢索紀錄〉 deleted outright, so there is no audit trail (it used to be *retitled*, which is now `SECT-01` — the table is found by its header either way) | exit 1, `STRUCT-01` |
| `trace_section_renamed.md` | 〈六、檢索紀錄〉 retitled 〈六、附錄：查詢筆記〉; the 22 log rows still parse | exit 1, `SECT-01` |
| `consensus_section_renamed.md` | 〈一、領域共識與未被質疑的預設〉 retitled; the three assumptions still parse and no G3 candidate is told its input is missing | exit 1, `SECT-01` |
| `no_tool_tier.md` | 〈文獻工具〉 header declaration is a placeholder | exit 1, `STRUCT-02` |
| `count_mismatch.md` | Candidate `C03`'s whole block is deleted from section 二 while the block still claims 3 survivors | exit 1, `COUNT-01` |
| `recon_mismatch.md` | `C06`'s row is deleted from section 三 while the block still claims 待確認 3 | exit 1, `RECON-01` |
| `bad_verdict.md` | Verdict `NOVEL`, outside the vocabulary | exit 1, `VERDICT-01` |
| `done_in_survivors.md` | A `DONE` verdict sits in the survivor list | exit 1, `VERDICT-02` |
| `assumption_no_frame.md` | `A2`'s frame in the block loses its `Mp` field (and with it, silently, the two arithmetic rules that depend on it — which is why they are skipped rather than piled on) | exit 1, `ASSUM-01` |
| `inherited_framed_partial.md` | `A3` is still `inherited_framed` but its frame loses `K` | exit 1, `ASSUM-01` |
| `impression_as_g3.md` | Candidate 1's G3 is repointed at `A3`, whose `status` is `impression` | exit 1, `ASSUM-02` |
| `inherited_unframed_as_g3.md` | C02 inverts `A2`, whose `status` is `inherited` — a wall carried over from the landscape report whose frame was never paid | exit 1, `ASSUM-02` |
| `missing_evidence_field.md` | Survivor has no search-evidence field at all | exit 1, `EVID-01` |
| `no_evidence.md` | Survivor's search-evidence field is the placeholder 「同上」 | exit 1, `EVID-02` |
| `vague_evidence.md` | Search evidence is prose with no verbatim query | exit 1, `EVID-03` |
| `neighbour_no_id.md` | Nearest study is named but carries no identifier | exit 1, `NEIGH-01` |
| `unnamed_kill.md` | The `DONE` row names no literature | exit 1, `KILL-01` |
| `crowded_two_papers.md` | `CROWDED` names only two papers | exit 1, `KILL-02` |
| `done_no_quote.md` | `DONE` reason carries no verbatim abstract quote | exit 1, `KILL-03` |
| `kill_no_identifier.md` | Key literature named, identifier cell blank | exit 1, `ID-01` |
| `untraced_candidate.md` | C03 has no row in 〈檢索紀錄〉 | exit 1, `TRACE-01` |
| `assumption_untraced.md` | The `第1步-推翻A1` row is deleted while the block still declares `A1` as `framed` — the other arm of the interlock, whose exemption side `inherited_unframed_ok.md` pins | exit 1, `TRACE-01` |
| `trace_placeholder_query.md` | A 〈檢索紀錄〉 query cell is 「（略）」 | exit 1, `TRACE-02` |
| `assertive_language.md` | 「沒有人做過…目前不存在相關研究…可以確定是新的」 | exit 1, `LANG-01` ×4 (one clause matches two patterns) |
| `no_search_with_verdicts.md` | Header declares no search ran, verdicts and kills filled anyway | exit 1, `TIER-01` |
| `good_landscape.md` | — (the landscape baseline; second hand-written base) | exit 0, no findings |
| `landscape_stray_block.md` | A landscape report ends with an `rgh-block` — a region no rule validates, and which used to be stripped out of the vocabulary scan as well | exit 1, `BLOCK-01` |
| `landscape_no_disclaimer.md` | The 〈這份報告不做什麼〉 header line is deleted | exit 1, `LHEAD-01` |
| `landscape_verdict_word.md` | §三 concludes that a combination is `CROWDED` | exit 1, `LVOCAB-01` |
| `landscape_no_cost.md` | F5 states what it buys; its 〈付出什麼〉 is 「—」 | exit 1, `LCOST-01` |
| `landscape_status_asserted.md` | F5 is 活躍 「近三年明顯在加速」 with no search clause in the field | exit 1, `LSTAT-01` |
| `landscape_orphan_assumption.md` | W3 drops F6-a (and its 家族數 with it), so that assumption reaches no wall | exit 1, `LWALL-01` |
| `landscape_assertive.md` | A landscape report writes 「這種疊法沒有人做過」 | exit 1, `LANG-01` |
| `landscape_no_tier.md` | A landscape report's 〈文獻工具〉 declaration is a placeholder | exit 1, `STRUCT-02` |
| `candidate_head_unreadable.md` | 候選 3's heading numbers itself 「候選三」, so the heading does not parse (the `C03` id still does) | exit 1, `PARSE-01` |
| `kill_row_short.md` | C08's eliminated row is one cell short of its header | exit 1, `PARSE-01` |
| `landscape_wall_row_short.md` | W3's wall row is one cell short of its header, so 〈家族數〉 reads 〈性質〉 and the arithmetic is skipped | exit 1, `PARSE-01` |
| `kill_row_no_verdict.md` | C07's eliminated row has an empty 判定 cell | exit 1, `VERDICT-01` |
| `candidate_head_no_keyword.md` | 候選 1's heading is `### C01：<題目>` — no 候選 keyword, so neither the heading pattern nor the look-alike sees it | exit 1, `PARSE-01` |
| `fenced_assumption_line.md` | `A3`'s line is wrapped in a ```` ```text ```` fence — unchanged, still in section 一, and now rendering as code rather than as report content | exit 1, `PARSE-01` |
| `tilde_fenced_assumption_line.md` | the same line in a `~~~` fence — the same defect through the other fence character, which renders identically | exit 1, `PARSE-01` |
| `family_head_no_id.md` | F1's family heading loses its `F1`; the family is rebuilt from its 默默預設 ids | exit 1, `PARSE-01` |
| `glance_row_lost_pipe.md` | the glance table's F1 row loses its leading `\|` — one character, and it used to be silent | exit 1, `PARSE-01` |
| `glance_table_gone.md` | 〈一、一眼表〉 keeps its heading, the table under it is gone (what a paste from a rendered view looks like) | exit 1, `PARSE-01` |
| `landscape_no_glance.md` | 〈一、一眼表〉 removed entirely — the document-level arm, which is what survives a rename and a de-piped table at once | exit 1, `PARSE-01` |
| `landscape_section_renamed.md` | 〈一、一眼表〉 retitled 〈一、總覽表〉; all seven glance rows still parse | exit 1, `SECT-01` |

### Six fixtures were deleted, and here is each one

A fixture that tests a rule which no longer exists is not neutral: it is a claim about coverage that nobody will re-check. All six are recorded rather than dropped, because "the case became unreachable" and "somebody deleted an inconvenient test" look identical in a diff.

| Deleted | What it pinned | Why it is gone |
|---|---|---|
| **count_inverted.md** (deleted) | `COUNT-02`, 生成 ≥ 存活 | The rule is now **tautological**: `generated == survived + pending + killed` over non-negative integers implies it. A check that cannot fire cannot be pinned by a fixture, and this repo's convention is that an unpinnable id should not exist. The arithmetic itself is pinned by `block_settlement_mismatch.md` under `BLOCK-01` |
| **assumption_unreadable_line.md** (deleted) | The assumption line's bracket label running into the body with no delimiter | Unreachable: no line shape is parsed. Replayed — the same edit is now green, correctly, because nothing is hidden by it |
| **assumption_em_dash_separator.md** (deleted) | The em-dash separator that round 3's lookahead made invisible to both patterns | Unreachable, same reason. This was the fixture that proved the round-3 regression; the regression's whole mechanism is deleted, and 〈The scorecard〉 above records the replay (row R4) |
| **assumption_prose_mention.md** (deleted) | The **deliberate false red**: a sentence *about* two assumptions, flagged as unreadable | The false red was the price of the look-alike detector. The detector is gone, so the price is not being paid, and there is nothing left to keep red. The residual token scan in `ANCHOR-01` is one-way and only fires on an id the block does not have — pinned instead by `anchor_orphan_assumption.md` |
| **assumption_two_brackets_ok.md** (deleted) | Two adjacent bracket labels parsing | Unreachable: brackets are not parsed |
| **assumption_fullwidth_bracket_ok.md** (deleted) | Full-width ［］ brackets parsing, a promise this page made and the pattern did not keep | Unreachable, and **the promise is withdrawn too** — this page no longer claims anything about which bracket pairs are accepted, because nothing reads them. Withdrawing the sentence matters as much as deleting the fixture: a stale guarantee is what made that false red possible in the first place |

**`assumption_blockquote_ok.md` was kept**, and what it pins got stronger rather than weaker. It rewrites `A3`'s prose line into the blockquote form `SKILL.md` 第 1 步 puts on screen, and it must stay green — but the claim is no longer "the parser accepts this shape", it is "**no** shape decides anything". A green run is exactly what a silently dropped line used to produce, so `self_test.py` reads the file back and asserts three things: `A3` is in the block, it is classified `inherited_framed`, and a G3 candidate actually names it. It also asserts no `ANCHOR-01` — the containment normalization has to eat the `> ` prefix, or the rewrite would be a false red.

**The unreadable-input fixtures pin the other half of the promise.** Two of them (`kill_row_short.md`, `landscape_wall_row_short.md`) are the same defect in the two modes, which is what makes `PARSE-01`'s `both` an assertion rather than a claim — the same job `landscape_assertive.md` and `landscape_no_tier.md` do for `LANG-01` and `STRUCT-02`. `candidate_head_unreadable.md` is the one worth reading twice: before the fix it raised `COUNT-01` saying 「宣告存活 3 個，實際只有 2 個」, and the cheapest way to make that green is to change the 3 to a 2 — which deletes a candidate that was written, from the one interlock that exists to stop candidates disappearing. It now raises `PARSE-01` alone, because the candidate is registered from the `C03` in its heading and the counts stay right. (Weaken the heading fallback in a mutant and `COUNT-01` takes over again — `mutation_test.py -v` prints it as the backstop, which is the misleading message the fix removed.)

**The four bridge fixtures exist because nothing covered the bridge.** `SKILL.md` makes the landscape report's wall table the input to the hunt's 第 1 步, and that hand-off is the whole justification for having two modes — yet the checker's assumption-line pattern required the colon to follow the id directly, so every form the spec prescribes failed to parse. (That pattern then failed a second time, in the same mechanism, on the blockquoted and two-bracket forms — which is why the sweep above exists and why the rule is now stated as a property of every parser rather than a fix to one of them.) The consequences were both silent: an inherited assumption was invisible to the parser, therefore neither counted nor checked; and walking the full permitted path — pay the frame on one wall, relabel it 已補取樣框, invert it with a G3 candidate — made the checker reject a conformant report with 「G3 候選指到第一節沒有的預設 A3」, a message that sends the reader looking for a line that is in fact right there. Every suite was green throughout, which is the point: no fixture had ever put a 〔承接自地形 …〕 label in front of the checker. The four cover the two-by-two — unframed/framed × feeding G3 or not — so neither semantics can regress without a red run, and the two red ones pin the *messages* as well as the ids. **The bridge is now carried by `status` rather than by a label**, which removes the parsing failure that produced all of the above; the four fixtures stay, because the *semantics* they pin — who may feed G3, who owes a search-log row — are unchanged and still worth a red run each.

**The interlock is asserted from both sides, not narrated.** `SKILL.md`〈互鎖的例外〉 exempts an `inherited` assumption from needing a row in section 六 and requires one from a `framed` or `inherited_framed` assumption. Neither half is proved by a green run on its own: an exemption is indistinguishable from a rule that never fires, and a requirement is indistinguishable from a fixture that happens to satisfy it. So `self_test.py` reads the bridge fixtures back and asserts both directions — that each contains an `inherited` assumption with **no** search-log row naming it, and that `good_inherited_report.md`'s `framed`/`inherited_framed` assumptions each **do** have one — while `assumption_untraced.md` is the red fixture for the requirement and `mutation_test.py` weakens that arm specifically. Without the positive half, someone could disable the arm entirely and every suite would stay green.

`landscape_assertive.md` and `landscape_no_tier.md` are not new rules. They are what makes `both` in the mode column an assertion rather than a claim: without them, "`LANG-01` and `STRUCT-02` also apply to landscape reports" would be a sentence in this file that no fixture had ever tested, and a dispatch bug that skipped them entirely would leave every suite green.

### The degraded mode must also be green

`SKILL.md` specifies a *second* compliant output: when no search backend is available at all, the report declares 「本次未執行任何檢索…」, emits **zero** `### 候選` blocks, puts every candidate in 待確認 with the state 〔未驗證〕, and fills in no verdict anywhere. **It still writes a block** — `settlement` with zeroes where they belong, and every assumption `impression`, because tier 3 has no abstract-layer tool and therefore no legal `framed`. An empty `"assumptions": []` is an honest statement; omitting the block is not one, which is why there is no exemption for this mode either. An earlier version of this checker made that mode unsatisfiable — omitting the verdict raised `VERDICT-01`, writing 未驗證 raised `VERDICT-02`, and writing `OPEN` raised `TIER-01`, with `EVID-03` and `TRACE-01` firing on top regardless. A checker that cannot be satisfied by its own specification trains the model to ignore it, which is worse than having no checker.

So `format_check.py` recognises the degraded mode and suspends the checks that presuppose a search actually happened (`EVID-03`, `TRACE-01`, the missing-field arms of `VERDICT-01` and `NEIGH-01`, and `VERDICT-02`). `TIER-01` stays armed and now covers the kill table as well — a report that admits it ran no search but eliminates six candidates anyway is exactly the dishonesty this mode invites, and `no_search_with_verdicts.md` guards it.

**Every bibliographic reference in the fixtures is synthetic.** Authors are `Author A` … `Author CH` (`good_report.md` and everything derived from it use `A`–`R`; `chinese_index_na.md` has its own `S`–`AA` namespace, `good_landscape.md` a third one, `BA`–`BZ`, and `good_inherited_report.md` a fourth, `CA`–`CH`, so no two baselines ever collide); identifiers use the Crossref test prefix `10.5555`. One place falls outside that scheme: the NDLTD／Airiti row in `good_report.md`'s search log (and in every fixture derived from it) lists three invented zh-TW thesis titles with no `Author X` label and no system number, so neither tell applies to them — what covers them is the per-file banner at the top of the fixture, which says every reference in the file is fictional. Nothing in `fixtures/` corresponds to a real publication, and nothing in it should ever be cited.

### Choosing the fixture topic

The base corpus is about urban park green space and residents' physical activity. Three properties make it work as a test asset: the field has enough real international literature that a `CROWDED` or `DONE` verdict reads as plausible rather than invented; it has an obvious locally-bounded variant (shade in Taiwanese neighbourhood parks) that genuinely could only be settled in NDLTD / Airiti, so the Chinese-index path is exercised by a candidate that really needs it instead of by a decoration; and it is far enough from measurement-heavy CS that the assumption lines about exposure metrics and self-report validity are the field's own arguments, not borrowed ones.

`good_landscape.md` stays inside that same corpus deliberately: it maps the *measurement* families for green-space exposure (satellite indices, GIS buffers, street-view scoring, questionnaires, GPS traces, footfall counters, on-site audits). Two things fall out of that choice. The families are genuinely distinguished by their trade-offs rather than their names, which is the property `SKILL.md` asks for and the hardest one to fake in a fixture; and its wall W1 is the same proposition as `good_report.md`'s 預設 A1, so the pair also demonstrates the bridge the two modes are supposed to form — without either file claiming to be the other's output. What it deliberately is **not** is the domain of the live trial (image recognition), for the reason immediately below.

**The rule this encodes: a test corpus must not carry anyone's unpublished research direction.** Fixtures are the part of a repo people read most carefully, because they are the part that shows what the tool considers a good report — so a fixture built on a topic someone on the project is actively researching publishes that work by the back door. It also makes the corpus worse as a test: a topic the author knows intimately gets written with private context a reader cannot check, and plausibility to the author stops being evidence of plausibility to anyone else. Pick a topic with a real public literature that nobody on the project is working on.

## doc_scan.py — the docs must describe this repo, not the previous one

```bash
python evals/doc_scan.py
```

Everything else in this directory checks a *report*. This one checks the *documentation*, and it is the reason a stale README is a failing gate rather than a footnote. It derives the numbers first — check count from `CHECK_DESCRIPTIONS`, fixture count from `fixtures/` and `self_test.EXPECTED`, mutant count from `MUTATIONS` — and then reads `SKILL.md`, both top-level READMEs, this file, `references/` and `examples/` looking for claims that no longer match:

1. every `` `path.md` `` / `` `path.py` `` mentioned in prose actually exists;
2. every `python evals/…` / `python scripts/…` command tells the reader to run a file that is there;
3. every relative markdown link resolves;
4. the two READMEs still have the same number of `##` sections — the mirror is a promise, and a section that exists in one language only is a promise made to half the readers;
5. written-down counts (checks, fixtures, mutants, generators, template sections) match the derived ones, in both `N checks` and `N 條規則` shapes, and **both** output templates in `SKILL.md` — the gap report's and the landscape report's — still have the same `## ` section names as their baseline fixtures;
6. the verdict vocabulary in `SKILL.md` matches the one `format_check.py` accepts, the five 〈狀態〉 values likewise, and the 〈這份報告不做什麼〉 fixed sentence is byte-identical in both places — a one-character drift there would make every conformant landscape report fail `LHEAD-01`, with the report right and the checker wrong;
   - **and the same for the block**: the fence tag `json rgh-block`, the schema string `rgh-block/1` and all four `status` values appear verbatim in `SKILL.md`, and **the example block `SKILL.md` prints is run through `validate_block` itself**. That last one is the strongest coupling in the file — a report copied from the specification has to be a report the checker accepts, and a spec whose own example fails validation is a spec nobody can follow;
7. the two suites' coverage assertions still hold;
8. no local absolute path (`C:\Users\<name>\…`, `/home/<name>/…`) has leaked into any text file — the same class `zip_check.py` catches in the package, caught earlier;
9. how much of the working tree `git ls-files` actually covers, since `scripts/package_skill.py` packages from that list and silently ships whatever is committed.

Exit `1` on any mismatch, item 8 included — a leaked path fails the run here exactly as it does in the package. Item 9 is the exception: it prints as an advisory, because uncommitted files are a packaging hazard rather than a documentation defect. Read the whole output, not only the exit code.

What it is for: the counts in the top-level READMEs went stale by three and four while every suite in this directory stayed green, because until this script existed nothing in the repo read the prose. Eight occurrences — four claims, mirrored in both languages — and a reader had no way to know.

## zip_check.py

Before sharing a packaged skill (uploading to ChatGPT, sending it to someone):

```bash
python evals/zip_check.py research-gap-hunter.zip
```

Scans the archive for API keys, personal email addresses, machine-specific absolute paths, and stray `.env` / `.git` / `__pycache__` entries, and exits non-zero on a hit so `scripts/package_skill.py` deletes the archive instead of handing it over. Manual review misses these; a regex does not. This one matters more here than it looks: gap-hunter's docs quote clone paths, so the `C:\Users\…` pattern is the one most likely to fire.

## Each bad fixture must raise exactly one check

`self_test.py` asserts the *set* of check ids, not merely that the run went red. If a single broken dimension starts raising two ids, that is a defect in the checks — overlapping rules make the output undiagnosable — and it must be fixed in `format_check.py`, not absorbed into the expectations. The self-test also asserts that the human-readable mode agrees with `--json` on the exit code, that every finding has a valid line number, that each generated fixture differs from **its own** baseline by no more than two lines (the mapping comes from `make_fixtures.derived_bases()`, not a second copy kept here), and that the fixtures on disk are byte-identical to what the generator produces. It additionally asserts the inherited-unframed search-log exemption directly against the fixtures, as described under 〈fixtures/〉.

`self_test.py` also asserts **coverage**: every id in `CHECK_DESCRIPTIONS` must own a fixture. A check with no fixture is not a tested check — it is only a check that `good_report.md` happens not to trip — and the suite fails rather than letting that pass silently. `mutation_test.py` asserts the mirror image: every declared id must own a mutant.

## mutation_test.py — proving the rule is what caught it

A green self-test proves a bad fixture goes red. It does not prove *which rule* turned it red. A fixture can trip three rules at once while the rule you believe is guarding it never executes; the suite still passes, and the coverage table lies.

So `mutation_test.py` weakens each rule's **condition** — not its `self.add` line — in a throwaway copy of `format_check.py` under `tempfile.mkdtemp()`, and re-runs that rule's fixture:

- the target id disappears → that rule is what caught it (**PASS**)
- the target id survives → something else was catching it and the rule is unproven (**FAIL**)

Mutating the condition rather than the reporting line is the whole point: deleting a `self.add` obviously removes its finding and proves nothing. Every mutant is also run against **all three** clean baselines — `good_report.md`, `good_landscape.md` and `good_inherited_report.md` — because a weakening that turns a compliant report red has damaged a different rule, and with three document shapes a mutation can now damage the shape it was not aimed at. The real `format_check.py` is opened read-only throughout.

Where a rule has several arms that all fire on one fixture, the mutation targets the rule's **guard** instead of a single arm — `ASSUM-01`'s frame mutant skips the whole ten-field validation, `TIER-01`'s mutant disarms the degraded-mode branch, `RECON-01`'s mutant back-fills the settlement numbers from the actual row counts. That is still a condition, and it still proves the rule is load-bearing.

Several mutants weaken the *parser's* self-awareness rather than a report rule: the column-count comparison, the candidate-heading fallback, the blank-verdict arm. Each returns the checker to the behaviour it had before the round that fixed it, which is the useful thing to see in a mutation run — `PARSE-01`'s heading mutant makes `candidate_head_unreadable.md` red under `COUNT-01` instead, printed as a backstop under `-v`, and that backstop is precisely the misleading message the fix removed. The blank-verdict mutant restores the old `continue`, because merely disabling the report there would let the vocabulary arm catch the same row and prove nothing.

**The block's mutants are worth reading, because each one restores a specific rejected design.** `BLOCK-01`'s first widens the rule to "if there is no block, skip the block rules" — that is the degraded mode the design rejected, and the fixture goes green under it, which is exactly what the argument against a degraded mode predicts. Its second stops requiring exactly one block, which puts the decoy class back. `ANCHOR-01`'s comment-stripping mutant deletes one line of normalization and the HTML-comment fixture goes green — the shortest possible demonstration that that step is load-bearing rather than tidiness.

`BLOCK-01`, `ANCHOR-01`, `ASSUM-01`, `ASSUM-02`, `TRACE-01`, `SECT-01` and `PARSE-01` each own more than one mutant, one per arm, because the branches are genuinely separate rather than another way of reaching the same code. A check id may appear more than once in `MUTATIONS`; the coverage assertion deduplicates. One arm deliberately has **no** mutant: a block that is not legal JSON. Every weakening of that path is caught immediately afterwards by the schema check — `validate_block(None)` reports that the top level is not an object — so the id never disappears and a mutant there would fail for the right reason rather than prove anything. Defence in depth is fine; recording where it is, so the next person does not read the gap as an oversight, is the point.

Rules backstopped by another are reported rather than hidden: `-v` prints which id took over after the target was weakened. Defence in depth is fine. Not knowing which layer fires first is not.

## Adding a check

0. First decide **which side it belongs on**. If the rule needs to *compute* something — count, reconcile, compare numbers, check an enumerated value or a reference — its data belongs in the block and the rule is EXACT. If it only needs to know whether something was written, it is a containment question over the prose and no schema change is needed. Adding a field to the block that no rule does arithmetic on is how a nine-line block becomes a fifty-line one; if you find yourself past twenty lines, the answer was containment.
1. Add the rule to `CHECK_DESCRIPTIONS` with a prefixed id, then implement it in the matching `check_*` method — on `Checker` for a gap rule, on `LandscapeChecker` for a landscape one, on `BaseChecker` only if it genuinely applies to both. Block-shape rules go through `validate_block`, which returns `(check_id, locator, message)` triples so `doc_scan.py` can reuse it on `SKILL.md`'s own example.
2. Add a fixture spec to `FIXTURES_SPEC` (gap), `LAND_FIXTURES_SPEC` (landscape) or `INHERIT_FIXTURES_SPEC` (a hunt report that inherited walls) in `make_fixtures.py` breaking that one dimension only, and run the generator. Break exactly the arm you intend to mutate: a fixture that trips two arms of the same rule needs a mutation that disarms both, and the honest way to get one is usually a narrower fixture rather than a wider mutation. **Edit the block or the prose, not both** — see 〈fixtures/〉 for why that is enforced rather than advised.
3. Add the row to `EXPECTED` in `self_test.py`.
4. Add the row to `MUTATIONS` in `mutation_test.py`, weakening the new rule's **condition**.
5. Run `python evals/self_test.py` then `python evals/mutation_test.py`. The new fixture must raise exactly its own id, every existing fixture must be unchanged (an old fixture gaining a second id means the new check overlaps an old one), and the new mutant must turn its fixture green.

If the new rule can fire on the degraded no-search report, gate it on `self.rep.no_search_declared` — see the degraded-mode note above.

## Not covered here

No network, so nothing about search quality, coverage, or literature truth is tested — see the block at the top. There is also no test of the skill's *judgment*: whether the six generators produced good candidates, whether an elimination was correct, whether a survivor is genuinely novel. Those are not mechanically checkable and this directory does not pretend otherwise. The honest limitations section of the README says the same thing in the user's language.
